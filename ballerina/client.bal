// AUTO-GENERATED FILE. DO NOT MODIFY.
// This file is auto-generated by the Ballerina OpenAPI tool.

// Copyright (c) 2025, WSO2 LLC. (http://www.wso2.com).
//
// WSO2 LLC. licenses this file to you under the Apache License,
// Version 2.0 (the "License"); you may not use this file except
// in compliance with the License.
// You may obtain a copy of the License at
//
// http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing,
// software distributed under the License is distributed on an
// "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
// KIND, either express or implied.  See the License for the
// specific language governing permissions and limitations
// under the License.

import ballerina/data.jsondata;
import ballerina/http;
import ballerina/mime;

# The Docusign eSignature REST API provides you with a powerful, convenient, and simple Web services API for interacting with Docusign.
public isolated client class Client {
    final http:Client clientEp;
    # Gets invoked to initialize the `connector`.
    #
    # + config - The configurations to be used when initializing the `connector` 
    # + serviceUrl - URL of the target service 
    # + return - An error if connector initialization failed 
    public isolated function init(ConnectionConfig config, string serviceUrl = "https://www.docusign.net/restapi") returns error? {
        http:ClientConfiguration httpClientConfig = {auth: config.auth, httpVersion: config.httpVersion, http1Settings: config.http1Settings, http2Settings: config.http2Settings, timeout: config.timeout, forwarded: config.forwarded, followRedirects: config.followRedirects, poolConfig: config.poolConfig, cache: config.cache, compression: config.compression, circuitBreaker: config.circuitBreaker, retryConfig: config.retryConfig, cookieConfig: config.cookieConfig, responseLimits: config.responseLimits, secureSocket: config.secureSocket, proxy: config.proxy, socketConfig: config.socketConfig, validation: config.validation, laxDataBinding: config.laxDataBinding};
        self.clientEp = check new (serviceUrl, httpClientConfig);
    }

    # Retrieves the available REST API versions.
    #
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getServiceInformation(map<string|string[]> headers = {}) returns ServiceInformation|error {
        string resourcePath = string `/service_information`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Lists resources for REST version specified
    #
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getResourceInformation(map<string|string[]> headers = {}) returns ResourceInformation|error {
        string resourcePath = string `/v2.1`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates new accounts.
    #
    # + headers - Headers to be sent with the request 
    # + payload - New account definition used to create new accounts 
    # + return - A successful response or an error 
    remote isolated function createAccount(NewAccountDefinition payload, map<string|string[]> headers = {}) returns NewAccountSummary|error {
        string resourcePath = string `/v2.1/accounts`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves the account information for the specified account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getAccount(string accountId, map<string|string[]> headers = {}, *GetAccountQueries queries) returns AccountInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes the specified account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteAccount(string accountId, map<string|string[]> headers = {}, *DeleteAccountQueries queries) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Gets list of recurring and usage charges for the account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listBillingCharges(string accountId, map<string|string[]> headers = {}, *ListBillingChargesQueries queries) returns BillingChargeResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/billing_charges`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get a List of Billing Invoices
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listBillingInvoices(string accountId, map<string|string[]> headers = {}, *ListBillingInvoicesQueries queries) returns BillingInvoicesResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/billing_invoices`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves a billing invoice.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + invoiceId - The ID of the invoice
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getBillingInvoice(string accountId, string invoiceId, map<string|string[]> headers = {}) returns BillingInvoice|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/billing_invoices/${getEncodedUri(invoiceId)}`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Get a list of past due invoices.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listPastDueInvoices(string accountId, map<string|string[]> headers = {}) returns BillingInvoicesSummary|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/billing_invoices_past_due`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Gets payment information for one or more payments.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listPayments(string accountId, map<string|string[]> headers = {}, *ListPaymentsQueries queries) returns BillingPaymentsResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/billing_payments`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Posts a payment to a past due invoice.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Billing payment request used to post a payment to a past due invoice 
    # + return - A successful response or an error 
    remote isolated function createPayment(string accountId, BillingPaymentRequest payload, map<string|string[]> headers = {}) returns BillingPaymentResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/billing_payments`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Gets billing payment information for a specific payment.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + paymentId - The ID of the payment
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getPayment(string accountId, string paymentId, map<string|string[]> headers = {}) returns BillingPaymentItem|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/billing_payments/${getEncodedUri(paymentId)}`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Get Account Billing Plan
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getAccountBillingPlan(string accountId, map<string|string[]> headers = {}, *GetAccountBillingPlanQueries queries) returns AccountBillingPlanResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/billing_plan`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates an account billing plan.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - Billing plan information used to update an account billing plan 
    # + return - A successful response or an error 
    remote isolated function updateAccountBillingPlan(string accountId, BillingPlanInformation payload, map<string|string[]> headers = {}, *UpdateAccountBillingPlanQueries queries) returns BillingPlanUpdateResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/billing_plan`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Get credit card information
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getCreditCardInfo(string accountId, map<string|string[]> headers = {}) returns CreditCardInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/billing_plan/credit_card`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns downgrade plan information for the specified account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getDowngradeRequestBillingInfo(string accountId, map<string|string[]> headers = {}) returns DowngradeRequestBillingInfoResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/billing_plan/downgrade`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Queues downgrade billing plan request for an account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Downgrade billing plan information used to queue downgrade billing plan request for an account 
    # + return - A successful response or an error 
    remote isolated function downgradeBillingPlan(string accountId, DowngradeBillingPlanInformation payload, map<string|string[]> headers = {}) returns DowngradePlanUpdateResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/billing_plan/downgrade`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Reserved: Purchase additional envelopes.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Purchased envelopes information used to purchase additional envelopes 
    # + return - A successful response or an error 
    remote isolated function purchaseEnvelopes(string accountId, PurchasedEnvelopesInformation payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/billing_plan/purchased_envelopes`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Gets a list of brands.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listBrands(string accountId, map<string|string[]> headers = {}, *ListBrandsQueries queries) returns AccountBrands|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/brands`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates one or more brand profiles for an account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Brand used to create one or more brand profiles for an account 
    # + return - A successful response or an error 
    remote isolated function createBrands(string accountId, Brand payload, map<string|string[]> headers = {}) returns AccountBrands|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/brands`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes one or more brand profiles.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Brands request used to delete one or more brand profiles 
    # + return - A successful response or an error 
    remote isolated function deleteBrands(string accountId, BrandsRequest payload, map<string|string[]> headers = {}) returns AccountBrands|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/brands`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->delete(resourcePath, request, headers);
    }

    # Gets information about a brand.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + brandId - The ID of the brand
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getBrand(string accountId, string brandId, map<string|string[]> headers = {}, *GetBrandQueries queries) returns Brand|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/brands/${getEncodedUri(brandId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates an existing brand.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + brandId - The ID of the brand
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - Brand used to update an existing brand 
    # + return - A successful response or an error 
    remote isolated function updateBrand(string accountId, string brandId, Brand payload, map<string|string[]> headers = {}, *UpdateBrandQueries queries) returns Brand|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/brands/${getEncodedUri(brandId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes a brand.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + brandId - The ID of the brand
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteBrand(string accountId, string brandId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/brands/${getEncodedUri(brandId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Exports a brand.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + brandId - The ID of the brand
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function exportBrand(string accountId, string brandId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/brands/${getEncodedUri(brandId)}/file`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Gets a brand logo.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + brandId - The ID of the brand
    # + logoType - The type of logo. Valid values are:
    # - `primary` 
    # - `secondary` 
    # - `email`
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getBrandLogo(string accountId, string brandId, string logoType, map<string|string[]> headers = {}) returns byte[]|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/brands/${getEncodedUri(brandId)}/logos/${getEncodedUri(logoType)}`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates a brand logo.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + brandId - The ID of the brand
    # + logoType - The type of logo. Valid values are:
    # - `primary` 
    # - `secondary` 
    # - `email`
    # + headers - Headers to be sent with the request 
    # + payload - Brand logo binary Stream. Supported formats: JPG, GIF, PNG. Maximum file size: 300 KB. Recommended dimensions: 296 x 76 pixels (larger images will be resized). Changes may take up to one hour to display in all places 
    # + return - A successful response or an error 
    remote isolated function updateBrandLogo(string accountId, string brandId, string logoType, record {byte[] fileContent; string fileName;} payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/brands/${getEncodedUri(brandId)}/logos/${getEncodedUri(logoType)}`;
        http:Request request = new;
        request.setPayload(payload, "image/png");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes a brand logo.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + brandId - The ID of the brand
    # + logoType - The type of logo. Valid values are:
    # - `primary` 
    # - `secondary` 
    # - `email`
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteBrandLogo(string accountId, string brandId, string logoType, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/brands/${getEncodedUri(brandId)}/logos/${getEncodedUri(logoType)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Returns metadata about the branding resources for an account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + brandId - The ID of the brand
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listBrandResources(string accountId, string brandId, map<string|string[]> headers = {}) returns BrandResourcesList|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/brands/${getEncodedUri(brandId)}/resources`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a branding resource file.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + brandId - The ID of the brand
    # + resourceContentType - The type of brand resource file to return. Valid values are:
    # - `sending`
    # - `signing`
    # - `email`
    # - `signing_captive`
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getBrandResource(string accountId, string brandId, string resourceContentType, map<string|string[]> headers = {}, *GetBrandResourceQueries queries) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/brands/${getEncodedUri(brandId)}/resources/${getEncodedUri(resourceContentType)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates a branding resource file.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + brandId - The ID of the brand
    # + resourceContentType - The type of brand resource file that you are updating. Valid values are:
    # - `sending`
    # - `signing`
    # - `email`
    # - `signing_captive`
    # + headers - Headers to be sent with the request 
    # + payload - Resourcesresource content type body used to update a branding resource file 
    # + return - A successful response or an error 
    remote isolated function updateBrandResource(string accountId, string brandId, string resourceContentType, UpdateBrandResourceRequest payload, map<string|string[]> headers = {}) returns BrandResources|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/brands/${getEncodedUri(brandId)}/resources/${getEncodedUri(resourceContentType)}`;
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Returns a list of bulk send batch summaries. 
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listBulkSendBatches(string accountId, map<string|string[]> headers = {}, *ListBulkSendBatchesQueries queries) returns BulkSendBatchSummaries|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/bulk_send_batch`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Gets the status of a specific bulk send batch.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + bulkSendBatchId - The batch ID
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getBulkSendBatchStatus(string accountId, string bulkSendBatchId, map<string|string[]> headers = {}) returns BulkSendBatchStatus|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/bulk_send_batch/${getEncodedUri(bulkSendBatchId)}`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the name of a bulk send batch.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + bulkSendBatchId - The batch ID
    # + headers - Headers to be sent with the request 
    # + payload - Bulk send batch request used to update the name of a bulk send batch 
    # + return - A successful response or an error 
    remote isolated function updateBulkSendBatch(string accountId, string bulkSendBatchId, BulkSendBatchRequest payload, map<string|string[]> headers = {}) returns BulkSendBatchStatus|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/bulk_send_batch/${getEncodedUri(bulkSendBatchId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Applies a bulk action to all envelopes from a specified bulk send.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + bulkAction - The action to apply. Valid values:
    # * `resend`
    # * `correct`
    # * `void`
    # + bulkSendBatchId - The batch ID
    # + headers - Headers to be sent with the request 
    # + payload - Bulk send batch action request for this request 
    # + return - A successful response or an error 
    remote isolated function applyBulkSendBatchAction(string accountId, string bulkAction, string bulkSendBatchId, BulkSendBatchActionRequest payload, map<string|string[]> headers = {}) returns BulkSendBatchStatus|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/bulk_send_batch/${getEncodedUri(bulkSendBatchId)}/${getEncodedUri(bulkAction)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Gets envelopes from a specific bulk send batch.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + bulkSendBatchId - The batch ID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listBulkSendBatchEnvelopes(string accountId, string bulkSendBatchId, map<string|string[]> headers = {}, *ListBulkSendBatchEnvelopesQueries queries) returns EnvelopesInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/bulk_send_batch/${getEncodedUri(bulkSendBatchId)}/envelopes`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Gets bulk send lists.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listBulkSendLists(string accountId, map<string|string[]> headers = {}) returns BulkSendingListSummaries|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/bulk_send_lists`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a bulk send list.
    #
    # + accountId - The ID of the account
    # + headers - Headers to be sent with the request 
    # + payload - Bulk sending list used to create a bulk send list 
    # + return - A successful response or an error 
    remote isolated function createBulkSendList(string accountId, BulkSendingList payload, map<string|string[]> headers = {}) returns BulkSendingList|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/bulk_send_lists`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Gets a specific bulk send list.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + bulkSendListId - The GUID of the bulk send list. This property is created after you post a new bulk send list
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getBulkSendList(string accountId, string bulkSendListId, map<string|string[]> headers = {}) returns BulkSendingList|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/bulk_send_lists/${getEncodedUri(bulkSendListId)}`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates a bulk send list.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + bulkSendListId - The GUID of the bulk send list. This property is created after you post a new bulk send list
    # + headers - Headers to be sent with the request 
    # + payload - Bulk sending list used to update a bulk send list 
    # + return - A successful response or an error 
    remote isolated function updateBulkSendList(string accountId, string bulkSendListId, BulkSendingList payload, map<string|string[]> headers = {}) returns BulkSendingList|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/bulk_send_lists/${getEncodedUri(bulkSendListId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes a bulk send list.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + bulkSendListId - The GUID of the bulk send list. This property is created after you post a new bulk send list
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteBulkSendList(string accountId, string bulkSendListId, map<string|string[]> headers = {}) returns BulkSendingListSummaries|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/bulk_send_lists/${getEncodedUri(bulkSendListId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Creates a bulk send request.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + bulkSendListId - The GUID of the bulk send list. This property is created after you post a new bulk send list
    # + headers - Headers to be sent with the request 
    # + payload - Bulk send request used to create a bulk send request 
    # + return - A successful response or an error 
    remote isolated function sendBulkSendList(string accountId, string bulkSendListId, BulkSendRequest payload, map<string|string[]> headers = {}) returns BulkSendResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/bulk_send_lists/${getEncodedUri(bulkSendListId)}/send`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Creates a bulk send test.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + bulkSendListId - The GUID of the bulk send list. This property is created after you post a new bulk send list
    # + headers - Headers to be sent with the request 
    # + payload - Bulk send request used to create a bulk send test 
    # + return - A successful response or an error 
    remote isolated function testBulkSendList(string accountId, string bulkSendListId, BulkSendRequest payload, map<string|string[]> headers = {}) returns BulkSendTestResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/bulk_send_lists/${getEncodedUri(bulkSendListId)}/test`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes the signature for one or more captive recipient records.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + recipientPart - Signature is the only supported value. 
    # + headers - Headers to be sent with the request 
    # + payload - Captive recipient information for this request 
    # + return - A successful response or an error 
    remote isolated function deleteCaptiveRecipients(string accountId, string recipientPart, CaptiveRecipientInformation payload, map<string|string[]> headers = {}) returns CaptiveRecipientInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/captive_recipients/${getEncodedUri(recipientPart)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->delete(resourcePath, request, headers);
    }

    # Initiate a new chunked upload.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Chunked upload request used to initiate a new chunked upload 
    # + return - A successful response or an error 
    remote isolated function createChunkedUpload(string accountId, ChunkedUploadRequest payload, map<string|string[]> headers = {}) returns ChunkedUploadResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/chunked_uploads`;
        http:Request request = new;
        mime:Entity[] bodyParts = check createBodyParts(check jsondata:toJson(payload).ensureType());
        request.setBodyParts(bodyParts);
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Retrieves metadata about a chunked upload.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + chunkedUploadId - The ID of the chunked upload. 
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getChunkedUpload(string accountId, string chunkedUploadId, map<string|string[]> headers = {}, *GetChunkedUploadQueries queries) returns ChunkedUploadResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/chunked_uploads/${getEncodedUri(chunkedUploadId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Commit a chunked upload.
    #
    # + accountId - (Required) The external account number (int) or account ID GUID
    # + chunkedUploadId - (Required) The ID of the chunked upload to commit
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function commitChunkedUpload(string accountId, string chunkedUploadId, map<string|string[]> headers = {}, *CommitChunkedUploadQueries queries) returns ChunkedUploadResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/chunked_uploads/${getEncodedUri(chunkedUploadId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes a chunked upload.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + chunkedUploadId - The ID of the chunked upload. 
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteChunkedUpload(string accountId, string chunkedUploadId, map<string|string[]> headers = {}) returns ChunkedUploadResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/chunked_uploads/${getEncodedUri(chunkedUploadId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Add a chunk to an existing chunked upload.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + chunkedUploadId - The ID of the chunked upload. 
    # + chunkedUploadPartSeq - The sequence or order of the part in the chunked upload. By default, the sequence of the first part that is uploaded as part of the Create request is `0`.
    # **Note:** You can add parts out of order. However, the chunked upload must consist of a contiguous series of one or more parts before you can successfully commit it
    # + headers - Headers to be sent with the request 
    # + payload - Chunked upload request used to add a chunk to an existing chunked upload 
    # + return - A successful response or an error 
    remote isolated function updateChunkedUploadPart(string accountId, string chunkedUploadId, string chunkedUploadPartSeq, ChunkedUploadRequest payload, map<string|string[]> headers = {}) returns ChunkedUploadResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/chunked_uploads/${getEncodedUri(chunkedUploadId)}/${getEncodedUri(chunkedUploadPartSeq)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Get Connect configuration information.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listConnectConfigurations(string accountId, map<string|string[]> headers = {}) returns ConnectConfigResults|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/connect`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates a specified Connect configuration.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Connect custom configuration used to update a specified Connect configuration 
    # + return - A successful response or an error 
    remote isolated function updateConnectConfiguration(string accountId, ConnectCustomConfiguration payload, map<string|string[]> headers = {}) returns ConnectCustomConfiguration|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/connect`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Creates a Connect configuration.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Connect custom configuration used to create a Connect configuration 
    # + return - A successful response or an error 
    remote isolated function createConnectConfiguration(string accountId, ConnectCustomConfiguration payload, map<string|string[]> headers = {}) returns ConnectCustomConfiguration|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/connect`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Gets the details about a Connect configuration.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + connectId - The ID of the custom Connect configuration being accessed
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getConnectConfiguration(string accountId, string connectId, map<string|string[]> headers = {}) returns ConnectConfigResults|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/connect/${getEncodedUri(connectId)}`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes the specified Connect configuration.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + connectId - The ID of the custom Connect configuration being accessed
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteConnectConfiguration(string accountId, string connectId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/connect/${getEncodedUri(connectId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Returns all users from the configured Connect service.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + connectId - The ID of the custom Connect configuration being accessed
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listAllConnectUsers(string accountId, string connectId, map<string|string[]> headers = {}, *ListAllConnectUsersQueries queries) returns IntegratedConnectUserInfoList|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/connect/${getEncodedUri(connectId)}/all/users`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns users from the configured Connect service.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + connectId - The ID of the custom Connect configuration being accessed
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listConnectUsers(string accountId, string connectId, map<string|string[]> headers = {}, *ListConnectUsersQueries queries) returns IntegratedUserInfoList|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/connect/${getEncodedUri(connectId)}/users`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Republishes Connect information for the specified envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function retryConnectEnvelope(string accountId, string envelopeId, map<string|string[]> headers = {}) returns ConnectFailureResults|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/connect/envelopes/${getEncodedUri(envelopeId)}/retry_queue`;
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Submits a batch of historical envelopes for republish to a webhook.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Connect historical envelope republish for this request 
    # + return - A successful response or an error 
    remote isolated function publishHistoricalEnvelopes(string accountId, ConnectHistoricalEnvelopeRepublish payload, map<string|string[]> headers = {}) returns EnvelopePublishTransaction|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/connect/envelopes/publish/historical`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Republishes Connect information for multiple envelopes.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Connect failure filter used to republish connect information for multiple envelopes 
    # + return - A successful response or an error 
    remote isolated function retryConnectEnvelopes(string accountId, ConnectFailureFilter payload, map<string|string[]> headers = {}) returns ConnectFailureResults|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/connect/envelopes/retry_queue`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Gets the Connect failure log information.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listConnectFailures(string accountId, map<string|string[]> headers = {}, *ListConnectFailuresQueries queries) returns ConnectLogs|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/connect/failures`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a Connect failure log entry.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + failureId - The ID of the Connect post failure. Use `all` to delete all failures for the account
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteConnectFailure(string accountId, string failureId, map<string|string[]> headers = {}) returns ConnectDeleteFailureResult|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/connect/failures/${getEncodedUri(failureId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Gets the Connect log.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listConnectLogs(string accountId, map<string|string[]> headers = {}, *ListConnectLogsQueries queries) returns ConnectLogs|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/connect/logs`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a list of Connect log entries.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteConnectLogs(string accountId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/connect/logs`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Gets a Connect log entry.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + logId - The ID of the Connect log entry
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getConnectLog(string accountId, string logId, map<string|string[]> headers = {}, *GetConnectLogQueries queries) returns ConnectLog|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/connect/logs/${getEncodedUri(logId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a specified Connect log entry.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + logId - The ID of the Connect log entry. Use `all` to delete all entries for the account
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteConnectLog(string accountId, string logId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/connect/logs/${getEncodedUri(logId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Retrieves the Connect OAuth information for the account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getConnectOAuthConfig(string accountId, map<string|string[]> headers = {}) returns ConnectOAuthConfig|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/connect/oauth`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the existing Connect OAuth configuration for the account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Connect OAuth config used to update the existing Connect OAuth configuration for the account 
    # + return - A successful response or an error 
    remote isolated function updateConnectOAuthConfig(string accountId, ConnectOAuthConfig payload, map<string|string[]> headers = {}) returns ConnectOAuthConfig|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/connect/oauth`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Set up Connect OAuth for the specified account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Connect OAuth config used to set up Connect OAuth for the specified account 
    # + return - A successful response or an error 
    remote isolated function createConnectOAuthConfig(string accountId, ConnectOAuthConfig payload, map<string|string[]> headers = {}) returns ConnectOAuthConfig|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/connect/oauth`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Delete the Connect OAuth configuration.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteConnectOAuthConfig(string accountId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/connect/oauth`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Retrieves the Connect HMAC secrets for an account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listConnectSecrets(string accountId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/connect/secret`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Generates a new connect HMAC Secret.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function createConnectSecret(string accountId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/connect/secret`;
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes the connect HMAC Secret for specified account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + keyId - The ID of the Connect HMAC secret key to delete.
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteConnectSecret(string accountId, string keyId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/connect/secret/${getEncodedUri(keyId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Gets the default Electronic Record and Signature Disclosure for an account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getConsumerDisclosure(string accountId, map<string|string[]> headers = {}, *GetConsumerDisclosureQueries queries) returns AccountConsumerDisclosures|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/consumer_disclosure`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Gets the Electronic Record and Signature Disclosure for an account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + langCode - The code for the signer language version of the disclosure that you want to retrieve. The following languages are supported:
    # - Arabic (`ar`)
    # - Bulgarian (`bg`)
    # - Czech (`cs`)
    # - Chinese Simplified (`zh_CN`)
    # - Chinese Traditional (`zh_TW`)
    # - Croatian (`hr`)
    # - Danish (`da`)
    # - Dutch (`nl`)
    # - English US (`en`)
    # - English UK (`en_GB`)
    # - Estonian (`et`)
    # - Farsi (`fa`)
    # - Finnish (`fi`)
    # - French (`fr`)
    # - French Canadian (`fr_CA`)
    # - German (`de`)
    # - Greek (`el`)
    # - Hebrew (`he`)
    # - Hindi (`hi`)
    # - Hungarian (`hu`)
    # - Bahasa Indonesian (`id`)
    # - Italian (`it`)
    # - Japanese (`ja`)
    # - Korean (`ko`)
    # - Latvian (`lv`)
    # - Lithuanian (`lt`)
    # - Bahasa Melayu (`ms`)
    # - Norwegian (`no`)
    # - Polish (`pl`)
    # - Portuguese (`pt`)
    # - Portuguese Brazil (`pt_BR`)
    # - Romanian (`ro`)
    # - Russian (`ru`)
    # - Serbian (`sr`)
    # - Slovak (`sk`)
    # - Slovenian (`sl`)
    # - Spanish (`es`)
    # - Spanish Latin America (`es_MX`)
    # - Swedish (`sv`)
    # - Thai (`th`)
    # - Turkish (`tr`)
    # - Ukrainian (`uk`)
    # - Vietnamese (`vi`)
    # Additionally, you can automatically detect the browser language being used by the viewer and display the disclosure in that language by setting the value to `browser`
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getConsumerDisclosureByLanguage(string accountId, string langCode, map<string|string[]> headers = {}) returns AccountConsumerDisclosures|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/consumer_disclosure/${getEncodedUri(langCode)}`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the Electronic Record and Signature Disclosure for an account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + langCode - The code for the signer language version of the disclosure that you want to update. The following languages are supported:
    # - Arabic (`ar`)
    # - Bulgarian (`bg`)
    # - Czech (`cs`)
    # - Chinese Simplified (`zh_CN`)
    # - Chinese Traditional (`zh_TW`)
    # - Croatian (`hr`)
    # - Danish (`da`)
    # - Dutch (`nl`)
    # - English US (`en`)
    # - English UK (`en_GB`)
    # - Estonian (`et`)
    # - Farsi (`fa`)
    # - Finnish (`fi`)
    # - French (`fr`)
    # - French Canadian (`fr_CA`)
    # - German (`de`)
    # - Greek (`el`)
    # - Hebrew (`he`)
    # - Hindi (`hi`)
    # - Hungarian (`hu`)
    # - Bahasa Indonesian (`id`)
    # - Italian (`it`)
    # - Japanese (`ja`)
    # - Korean (`ko`)
    # - Latvian (`lv`)
    # - Lithuanian (`lt`)
    # - Bahasa Melayu (`ms`)
    # - Norwegian (`no`)
    # - Polish (`pl`)
    # - Portuguese (`pt`)
    # - Portuguese Brazil (`pt_BR`)
    # - Romanian (`ro`)
    # - Russian (`ru`)
    # - Serbian (`sr`)
    # - Slovak (`sk`)
    # - Slovenian (`sl`)
    # - Spanish (`es`)
    # - Spanish Latin America (`es_MX`)
    # - Swedish (`sv`)
    # - Thai (`th`)
    # - Turkish (`tr`)
    # - Ukrainian (`uk`)
    # - Vietnamese (`vi`)
    # Additionally, you can automatically detect the browser language being used by the viewer and display the disclosure in that language by setting the value to `browser`
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - Consumer disclosure used to update the Electronic Record and Signature Disclosure for an account 
    # + return - A successful response or an error 
    remote isolated function updateConsumerDisclosure(string accountId, string langCode, ConsumerDisclosure payload, map<string|string[]> headers = {}, *UpdateConsumerDisclosureQueries queries) returns ConsumerDisclosure|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/consumer_disclosure/${getEncodedUri(langCode)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Updates one or more contacts.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Contact mod request used to update one or more contacts 
    # + return - A successful response or an error 
    remote isolated function updateContacts(string accountId, ContactModRequest payload, map<string|string[]> headers = {}) returns ContactUpdateResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/contacts`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Add contacts to a contacts list.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Contact mod request used to add contacts to a contacts list 
    # + return - A successful response or an error 
    remote isolated function createContacts(string accountId, ContactModRequest payload, map<string|string[]> headers = {}) returns ContactUpdateResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/contacts`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes multiple contacts from an account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Contact mod request used to delete multiple contacts from an account 
    # + return - A successful response or an error 
    remote isolated function deleteContacts(string accountId, ContactModRequest payload, map<string|string[]> headers = {}) returns ContactUpdateResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/contacts`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->delete(resourcePath, request, headers);
    }

    # Gets one or more contacts.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + contactId - The ID of a contact person in the account's address book.
    # **Note:** To return all contacts, omit this parameter. It is not required
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getContact(string accountId, string contactId, map<string|string[]> headers = {}, *GetContactQueries queries) returns ContactGetResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/contacts/${getEncodedUri(contactId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a contact.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + contactId - The ID of a contact person in the account's address book
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteContact(string accountId, string contactId, map<string|string[]> headers = {}) returns ContactUpdateResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/contacts/${getEncodedUri(contactId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Gets a list of custom fields.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listAccountCustomFields(string accountId, map<string|string[]> headers = {}) returns AccountCustomFields|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/custom_fields`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates an account custom field.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - Custom field used to create an account custom field 
    # + return - A successful response or an error 
    remote isolated function createAccountCustomField(string accountId, CustomField payload, map<string|string[]> headers = {}, *CreateAccountCustomFieldQueries queries) returns AccountCustomFields|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/custom_fields`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Updates an account custom field.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + customFieldId - The ID of the custom field
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - Custom field used to update an account custom field 
    # + return - A successful response or an error 
    remote isolated function updateAccountCustomField(string accountId, string customFieldId, CustomField payload, map<string|string[]> headers = {}, *UpdateAccountCustomFieldQueries queries) returns AccountCustomFields|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/custom_fields/${getEncodedUri(customFieldId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes an account custom field.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + customFieldId - The ID of the custom field
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteAccountCustomField(string accountId, string customFieldId, map<string|string[]> headers = {}, *DeleteAccountCustomFieldQueries queries) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/custom_fields/${getEncodedUri(customFieldId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Search for specific sets of envelopes by using search filters.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listEnvelopes(string accountId, map<string|string[]> headers = {}, *ListEnvelopesQueries queries) returns EnvelopesInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - Envelope definition used to create an envelope 
    # + return - A successful response or an error 
    remote isolated function createEnvelope(string accountId, EnvelopeDefinition payload, map<string|string[]> headers = {}, *CreateEnvelopeQueries queries) returns EnvelopeSummary|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Gets the status of a single envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getEnvelope(string accountId, string envelopeId, map<string|string[]> headers = {}, *GetEnvelopeQueries queries) returns Envelope|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Send, void, or modify a draft envelope. Purge documents from a completed envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - A container used to send documents to recipients. The envelope carries information about the sender and timestamps to indicate the progress of the delivery procedure. It can contain collections of Documents, Tabs and Recipients 
    # + return - A successful response or an error 
    remote isolated function updateEnvelope(string accountId, string envelopeId, Envelope payload, map<string|string[]> headers = {}, *UpdateEnvelopeQueries queries) returns EnvelopeUpdateSummary|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Returns a list of envelope attachments associated with a specified envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listEnvelopeAttachments(string accountId, string envelopeId, map<string|string[]> headers = {}) returns EnvelopeAttachmentsResult|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/attachments`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Adds one or more envelope attachments to a draft or in-process envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - Envelope attachments request for this request 
    # + return - A successful response or an error 
    remote isolated function addEnvelopeAttachments(string accountId, string envelopeId, EnvelopeAttachmentsRequest payload, map<string|string[]> headers = {}) returns EnvelopeAttachmentsResult|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/attachments`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes one or more envelope attachments from a draft envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - Envelope attachments request used to delete one or more envelope attachments from a draft envelope 
    # + return - A successful response or an error 
    remote isolated function deleteEnvelopeAttachments(string accountId, string envelopeId, EnvelopeAttachmentsRequest payload, map<string|string[]> headers = {}) returns EnvelopeAttachmentsResult|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/attachments`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->delete(resourcePath, request, headers);
    }

    # Retrieves an envelope attachment from an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + attachmentId - The unique identifier for the attachment
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getEnvelopeAttachment(string accountId, string attachmentId, string envelopeId, map<string|string[]> headers = {}) returns byte[]|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/attachments/${getEncodedUri(attachmentId)}`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates an envelope attachment in a draft or in-process envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + attachmentId - The unique identifier for the attachment
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - Attachment used to update an envelope attachment in a draft or in-process envelope 
    # + return - A successful response or an error 
    remote isolated function updateEnvelopeAttachment(string accountId, string attachmentId, string envelopeId, Attachment payload, map<string|string[]> headers = {}) returns EnvelopeAttachmentsResult|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/attachments/${getEncodedUri(attachmentId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Gets the envelope audit events for an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listEnvelopeAuditEvents(string accountId, string envelopeId, map<string|string[]> headers = {}, *ListEnvelopeAuditEventsQueries queries) returns EnvelopeAuditEventResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/audit_events`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Gets a PDF transcript of all of the comments in an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getCommentsTranscript(string accountId, string envelopeId, map<string|string[]> headers = {}, *GetCommentsTranscriptQueries queries) returns http:Response|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/comments/transcript`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Gets the custom field information for the specified envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listEnvelopeCustomFields(string accountId, string envelopeId, map<string|string[]> headers = {}) returns CustomFieldsEnvelope|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/custom_fields`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates envelope custom fields in an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - Envelope custom fields used to update envelope custom fields in an envelope 
    # + return - A successful response or an error 
    remote isolated function updateEnvelopeCustomFields(string accountId, string envelopeId, EnvelopeCustomFields payload, map<string|string[]> headers = {}) returns EnvelopeCustomFields|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/custom_fields`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Creates envelope custom fields for an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - Envelope custom fields used to create envelope custom fields for an envelope 
    # + return - A successful response or an error 
    remote isolated function createEnvelopeCustomFields(string accountId, string envelopeId, EnvelopeCustomFields payload, map<string|string[]> headers = {}) returns EnvelopeCustomFields|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/custom_fields`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes envelope custom fields for draft and in-process envelopes.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - Envelope custom fields used to delete envelope custom fields for draft and in-process envelopes 
    # + return - A successful response or an error 
    remote isolated function deleteEnvelopeCustomFields(string accountId, string envelopeId, EnvelopeCustomFields payload, map<string|string[]> headers = {}) returns EnvelopeCustomFields|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/custom_fields`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->delete(resourcePath, request, headers);
    }

    # Returns sender fields for an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listEnvelopeDocGenFormFields(string accountId, string envelopeId, map<string|string[]> headers = {}) returns DocGenFormFieldResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/docGenFormFields`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates sender fields for an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - Doc gen form field request used to update sender fields for an envelope 
    # + return - A successful response or an error 
    remote isolated function updateEnvelopeDocGenFormFields(string accountId, string envelopeId, DocGenFormFieldRequest payload, map<string|string[]> headers = {}, *UpdateEnvelopeDocGenFormFieldsQueries queries) returns DocGenFormFieldResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/docGenFormFields`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Gets a list of documents in an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listEnvelopeDocuments(string accountId, string envelopeId, map<string|string[]> headers = {}, *ListEnvelopeDocumentsQueries queries) returns EnvelopeDocumentsResult|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/documents`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Adds one or more documents to an existing envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - Envelope definition used to add one or more documents to an existing envelope 
    # + return - A successful response or an error 
    remote isolated function addEnvelopeDocuments(string accountId, string envelopeId, EnvelopeDefinition payload, map<string|string[]> headers = {}) returns EnvelopeDocumentsResult|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/documents`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes documents from a draft envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - Envelope definition used to delete documents from a draft envelope 
    # + return - A successful response or an error 
    remote isolated function deleteEnvelopeDocuments(string accountId, string envelopeId, EnvelopeDefinition payload, map<string|string[]> headers = {}) returns EnvelopeDocumentsResult|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/documents`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->delete(resourcePath, request, headers);
    }

    # Retrieves a single document or all documents from an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The ID of the document to retrieve. Alternatively, you can use one of the following special keywords:
    # - `combined`: Retrieves all of the documents as a single PDF file.
    # When the `certificate` query parameter is **true,** the certificate of completion is included in the PDF file.
    # When the `certificate` query parameter is **false,** the certificate of completion is not included in the PDF file.
    # - `archive`: Retrieves a ZIP archive that contains all of the PDF documents and the certificate of completion.
    # - `certificate`: Retrieves only the certificate of completion as a PDF file.
    # - `portfolio`: Retrieves the envelope documents as a [PDF portfolio](https://helpx.adobe.com/acrobat/using/overview-pdf-portfolios.html)
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getEnvelopeDocument(string accountId, string documentId, string envelopeId, map<string|string[]> headers = {}, *GetEnvelopeDocumentQueries queries) returns http:Response|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/documents/${getEncodedUri(documentId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Adds or replaces a document in an existing envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The unique ID of the document within the envelope.
    # Unlike other IDs in the eSignature API,
    # you specify the `documentId` yourself.
    # Typically the first document has the ID
    # `1`, the second document `2`, and so on,
    # but you can use any numbering scheme
    # that fits within a 32-bit signed integer
    # (1 through 2147483647).
    # Tab objects have a `documentId` property
    # that specifies the document on which to place
    # the tab
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + request - Updated document content 
    # + return - A successful response or an error 
    remote isolated function updateEnvelopeDocument(string accountId, string documentId, string envelopeId, http:Request request, map<string|string[]> headers = {}) returns EnvelopeDocument|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/documents/${getEncodedUri(documentId)}`;
        // TODO: Update the request as needed;
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Gets the custom document fields from an  existing envelope document.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The unique ID of the document within the envelope.
    # Unlike other IDs in the eSignature API,
    # you specify the `documentId` yourself.
    # Typically the first document has the ID
    # `1`, the second document `2`, and so on,
    # but you can use any numbering scheme
    # that fits within a 32-bit signed integer
    # (1 through 2147483647).
    # Tab objects have a `documentId` property
    # that specifies the document on which to place
    # the tab
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listEnvelopeDocumentFields(string accountId, string documentId, string envelopeId, map<string|string[]> headers = {}) returns EnvelopeDocumentFields|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/documents/${getEncodedUri(documentId)}/fields`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates existing custom document fields in an existing envelope document.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The unique ID of the document within the envelope.
    # Unlike other IDs in the eSignature API,
    # you specify the `documentId` yourself.
    # Typically the first document has the ID
    # `1`, the second document `2`, and so on,
    # but you can use any numbering scheme
    # that fits within a 32-bit signed integer
    # (1 through 2147483647).
    # Tab objects have a `documentId` property
    # that specifies the document on which to place
    # the tab
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - Envelope document fields for this request 
    # + return - A successful response or an error 
    remote isolated function updateEnvelopeDocumentFields(string accountId, string documentId, string envelopeId, EnvelopeDocumentFields payload, map<string|string[]> headers = {}) returns EnvelopeDocumentFields|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/documents/${getEncodedUri(documentId)}/fields`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Creates custom document fields in an existing envelope document.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The unique ID of the document within the envelope.
    # Unlike other IDs in the eSignature API,
    # you specify the `documentId` yourself.
    # Typically the first document has the ID
    # `1`, the second document `2`, and so on,
    # but you can use any numbering scheme
    # that fits within a 32-bit signed integer
    # (1 through 2147483647).
    # Tab objects have a `documentId` property
    # that specifies the document on which to place
    # the tab
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - Envelope document fields used to create custom document fields in an existing envelope document 
    # + return - A successful response or an error 
    remote isolated function createEnvelopeDocumentFields(string accountId, string documentId, string envelopeId, EnvelopeDocumentFields payload, map<string|string[]> headers = {}) returns EnvelopeDocumentFields|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/documents/${getEncodedUri(documentId)}/fields`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes custom document fields from an existing envelope document.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The unique ID of the document within the envelope.
    # Unlike other IDs in the eSignature API,
    # you specify the `documentId` yourself.
    # Typically the first document has the ID
    # `1`, the second document `2`, and so on,
    # but you can use any numbering scheme
    # that fits within a 32-bit signed integer
    # (1 through 2147483647).
    # Tab objects have a `documentId` property
    # that specifies the document on which to place
    # the tab
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - Envelope document fields used to delete custom document fields from an existing envelope document 
    # + return - A successful response or an error 
    remote isolated function deleteEnvelopeDocumentFields(string accountId, string documentId, string envelopeId, EnvelopeDocumentFields payload, map<string|string[]> headers = {}) returns EnvelopeDocumentFields|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/documents/${getEncodedUri(documentId)}/fields`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->delete(resourcePath, request, headers);
    }

    # Retrieves the HTML definition used to generate a dynamically sized responsive document.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The GUID of the document.
    # Example: c671747c-xxxx-xxxx-xxxx-4a4a48e23744
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listEnvelopeDocumentHtmlDefinitions(string accountId, string documentId, string envelopeId, map<string|string[]> headers = {}) returns DocumentHtmlDefinitionOriginals|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/documents/${getEncodedUri(documentId)}/html_definitions`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns document page images based on input.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The unique ID of the document within the envelope.
    # Unlike other IDs in the eSignature API,
    # you specify the `documentId` yourself.
    # Typically the first document has the ID
    # `1`, the second document `2`, and so on,
    # but you can use any numbering scheme
    # that fits within a 32-bit signed integer
    # (1 through 2147483647).
    # Tab objects have a `documentId` property
    # that specifies the document on which to place
    # the tab
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listEnvelopePageImages(string accountId, string documentId, string envelopeId, map<string|string[]> headers = {}, *ListEnvelopePageImagesQueries queries) returns PageImages|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/documents/${getEncodedUri(documentId)}/pages`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a page from a document in an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The unique ID of the document within the envelope.
    # Unlike other IDs in the eSignature API,
    # you specify the `documentId` yourself.
    # Typically the first document has the ID
    # `1`, the second document `2`, and so on,
    # but you can use any numbering scheme
    # that fits within a 32-bit signed integer
    # (1 through 2147483647).
    # Tab objects have a `documentId` property
    # that specifies the document on which to place
    # the tab
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + pageNumber - The page number being accessed
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteEnvelopePage(string accountId, string documentId, string envelopeId, string pageNumber, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/documents/${getEncodedUri(documentId)}/pages/${getEncodedUri(pageNumber)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Gets a page image from an envelope for display.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The unique ID of the document within the envelope.
    # Unlike other IDs in the eSignature API,
    # you specify the `documentId` yourself.
    # Typically the first document has the ID
    # `1`, the second document `2`, and so on,
    # but you can use any numbering scheme
    # that fits within a 32-bit signed integer
    # (1 through 2147483647).
    # Tab objects have a `documentId` property
    # that specifies the document on which to place
    # the tab
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + pageNumber - The page number being accessed
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getEnvelopePageImage(string accountId, string documentId, string envelopeId, string pageNumber, map<string|string[]> headers = {}, *GetEnvelopePageImageQueries queries) returns byte[]|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/documents/${getEncodedUri(documentId)}/pages/${getEncodedUri(pageNumber)}/page_image`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Rotates page image from an envelope for display.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The unique ID of the document within the envelope.
    # Unlike other IDs in the eSignature API,
    # you specify the `documentId` yourself.
    # Typically the first document has the ID
    # `1`, the second document `2`, and so on,
    # but you can use any numbering scheme
    # that fits within a 32-bit signed integer
    # (1 through 2147483647).
    # Tab objects have a `documentId` property
    # that specifies the document on which to place
    # the tab
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + pageNumber - The page number being accessed
    # + headers - Headers to be sent with the request 
    # + payload - Page request used to rotate page image from an envelope for display 
    # + return - A successful response or an error 
    remote isolated function rotateEnvelopePageImage(string accountId, string documentId, string envelopeId, string pageNumber, PageRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/documents/${getEncodedUri(documentId)}/pages/${getEncodedUri(pageNumber)}/page_image`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Returns tabs on the specified page.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The unique ID of the document within the envelope.
    # Unlike other IDs in the eSignature API,
    # you specify the `documentId` yourself.
    # Typically the first document has the ID
    # `1`, the second document `2`, and so on,
    # but you can use any numbering scheme
    # that fits within a 32-bit signed integer
    # (1 through 2147483647).
    # Tab objects have a `documentId` property
    # that specifies the document on which to place
    # the tab
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + pageNumber - The page number being accessed
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listEnvelopePageTabs(string accountId, string documentId, string envelopeId, string pageNumber, map<string|string[]> headers = {}) returns EnvelopeDocumentTabs|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/documents/${getEncodedUri(documentId)}/pages/${getEncodedUri(pageNumber)}/tabs`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a preview of the responsive version of a document.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The unique ID of the document within the envelope.
    # Unlike other IDs in the eSignature API,
    # you specify the `documentId` yourself.
    # Typically the first document has the ID
    # `1`, the second document `2`, and so on,
    # but you can use any numbering scheme
    # that fits within a 32-bit signed integer
    # (1 through 2147483647).
    # Tab objects have a `documentId` property
    # that specifies the document on which to place
    # the tab
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - Document HTML definition used to create a preview of the responsive version of a document 
    # + return - A successful response or an error 
    remote isolated function createDocumentResponsivePreview(string accountId, string documentId, string envelopeId, DocumentHtmlDefinition payload, map<string|string[]> headers = {}) returns DocumentHtmlDefinitions|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/documents/${getEncodedUri(documentId)}/responsive_html_preview`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns the tabs on a document.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The unique ID of the document within the envelope.
    # Unlike other IDs in the eSignature API,
    # you specify the `documentId` yourself.
    # Typically the first document has the ID
    # `1`, the second document `2`, and so on,
    # but you can use any numbering scheme
    # that fits within a 32-bit signed integer
    # (1 through 2147483647).
    # Tab objects have a `documentId` property
    # that specifies the document on which to place
    # the tab
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listEnvelopeDocumentTabs(string accountId, string documentId, string envelopeId, map<string|string[]> headers = {}, *ListEnvelopeDocumentTabsQueries queries) returns EnvelopeDocumentTabs|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/documents/${getEncodedUri(documentId)}/tabs`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the tabs for document.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The unique ID of the document within the envelope.
    # Unlike other IDs in the eSignature API,
    # you specify the `documentId` yourself.
    # Typically the first document has the ID
    # `1`, the second document `2`, and so on,
    # but you can use any numbering scheme
    # that fits within a 32-bit signed integer
    # (1 through 2147483647).
    # Tab objects have a `documentId` property
    # that specifies the document on which to place
    # the tab
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - A list of tabs, which are represented graphically as symbols on documents at the time of signing. Tabs show recipients where to sign, initial, or enter data. They may also display data to the recipients 
    # + return - A successful response or an error 
    remote isolated function updateEnvelopeDocumentTabs(string accountId, string documentId, string envelopeId, Tabs payload, map<string|string[]> headers = {}) returns Tabs|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/documents/${getEncodedUri(documentId)}/tabs`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Adds tabs to a document in an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The unique ID of the document within the envelope.
    # Unlike other IDs in the eSignature API,
    # you specify the `documentId` yourself.
    # Typically the first document has the ID
    # `1`, the second document `2`, and so on,
    # but you can use any numbering scheme
    # that fits within a 32-bit signed integer
    # (1 through 2147483647).
    # Tab objects have a `documentId` property
    # that specifies the document on which to place
    # the tab
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - A list of tabs, which are represented graphically as symbols on documents at the time of signing. Tabs show recipients where to sign, initial, or enter data. They may also display data to the recipients 
    # + return - A successful response or an error 
    remote isolated function createEnvelopeDocumentTabs(string accountId, string documentId, string envelopeId, Tabs payload, map<string|string[]> headers = {}) returns Tabs|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/documents/${getEncodedUri(documentId)}/tabs`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes tabs from a document in an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The unique ID of the document within the envelope.
    # Unlike other IDs in the eSignature API,
    # you specify the `documentId` yourself.
    # Typically the first document has the ID
    # `1`, the second document `2`, and so on,
    # but you can use any numbering scheme
    # that fits within a 32-bit signed integer
    # (1 through 2147483647).
    # Tab objects have a `documentId` property
    # that specifies the document on which to place
    # the tab
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - A list of tabs, which are represented graphically as symbols on documents at the time of signing. Tabs show recipients where to sign, initial, or enter data. They may also display data to the recipients 
    # + return - A successful response or an error 
    remote isolated function deleteEnvelopeDocumentTabs(string accountId, string documentId, string envelopeId, Tabs payload, map<string|string[]> headers = {}) returns Tabs|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/documents/${getEncodedUri(documentId)}/tabs`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->delete(resourcePath, request, headers);
    }

    # Gets the templates associated with a document in an existing envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The unique ID of the document within the envelope.
    # Unlike other IDs in the eSignature API,
    # you specify the `documentId` yourself.
    # Typically the first document has the ID
    # `1`, the second document `2`, and so on,
    # but you can use any numbering scheme
    # that fits within a 32-bit signed integer
    # (1 through 2147483647).
    # Tab objects have a `documentId` property
    # that specifies the document on which to place
    # the tab
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listDocumentTemplates(string accountId, string documentId, string envelopeId, map<string|string[]> headers = {}, *ListDocumentTemplatesQueries queries) returns TemplateInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/documents/${getEncodedUri(documentId)}/templates`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Adds templates to a document in an  envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The unique ID of the document within the envelope.
    # Unlike other IDs in the eSignature API,
    # you specify the `documentId` yourself.
    # Typically the first document has the ID
    # `1`, the second document `2`, and so on,
    # but you can use any numbering scheme
    # that fits within a 32-bit signed integer
    # (1 through 2147483647).
    # Tab objects have a `documentId` property
    # that specifies the document on which to place
    # the tab
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - Document template list used to add templates to a document in an envelope 
    # + return - A successful response or an error 
    remote isolated function addDocumentTemplates(string accountId, string documentId, string envelopeId, DocumentTemplateList payload, map<string|string[]> headers = {}, *AddDocumentTemplatesQueries queries) returns DocumentTemplateList|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/documents/${getEncodedUri(documentId)}/templates`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes a template from a document in an existing envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The unique ID of the document within the envelope.
    # Unlike other IDs in the eSignature API,
    # you specify the `documentId` yourself.
    # Typically the first document has the ID
    # `1`, the second document `2`, and so on,
    # but you can use any numbering scheme
    # that fits within a 32-bit signed integer
    # (1 through 2147483647).
    # Tab objects have a `documentId` property
    # that specifies the document on which to place
    # the tab
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteDocumentTemplate(string accountId, string documentId, string envelopeId, string templateId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/documents/${getEncodedUri(documentId)}/templates/${getEncodedUri(templateId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Gets the email setting overrides for an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getEmailSettings(string accountId, string envelopeId, map<string|string[]> headers = {}) returns EmailSettings|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/email_settings`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the email setting overrides for an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - A complex type that contains email settings 
    # + return - A successful response or an error 
    remote isolated function updateEmailSettings(string accountId, string envelopeId, EmailSettings payload, map<string|string[]> headers = {}) returns EmailSettings|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/email_settings`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Adds email setting overrides to an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - A complex type that contains email settings 
    # + return - A successful response or an error 
    remote isolated function createEmailSettings(string accountId, string envelopeId, EmailSettings payload, map<string|string[]> headers = {}) returns EmailSettings|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/email_settings`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes the email setting overrides for an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteEmailSettings(string accountId, string envelopeId, map<string|string[]> headers = {}) returns EmailSettings|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/email_settings`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Returns envelope tab data for an existing envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getEnvelopeFormData(string accountId, string envelopeId, map<string|string[]> headers = {}) returns EnvelopeFormData|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/form_data`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Gets the Original HTML Definition used to generate the Responsive HTML for the envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listEnvelopeHtmlDefinitions(string accountId, string envelopeId, map<string|string[]> headers = {}) returns DocumentHtmlDefinitionOriginals|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/html_definitions`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Gets envelope lock information.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getEnvelopeLock(string accountId, string envelopeId, map<string|string[]> headers = {}) returns EnvelopeLocks|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/lock`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates an envelope lock.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - Lock request used to update an envelope lock 
    # + return - A successful response or an error 
    remote isolated function updateEnvelopeLock(string accountId, string envelopeId, LockRequest payload, map<string|string[]> headers = {}) returns EnvelopeLocks|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/lock`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Locks an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - Lock request used to lock an envelope 
    # + return - A successful response or an error 
    remote isolated function createEnvelopeLock(string accountId, string envelopeId, LockRequest payload, map<string|string[]> headers = {}) returns EnvelopeLocks|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/lock`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes an envelope lock.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteEnvelopeLock(string accountId, string envelopeId, map<string|string[]> headers = {}) returns EnvelopeLocks|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/lock`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Gets envelope notification information.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getEnvelopeNotification(string accountId, string envelopeId, map<string|string[]> headers = {}) returns Notification|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/notification`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Sets envelope notifications for an existing envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - Envelope notification request used to set envelope notifications for an existing envelope 
    # + return - A successful response or an error 
    remote isolated function updateEnvelopeNotification(string accountId, string envelopeId, EnvelopeNotificationRequest payload, map<string|string[]> headers = {}) returns Notification|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/notification`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Gets the status of recipients for an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listEnvelopeRecipients(string accountId, string envelopeId, map<string|string[]> headers = {}, *ListEnvelopeRecipientsQueries queries) returns EnvelopeRecipients|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/recipients`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates recipients in a draft envelope or corrects recipient information for an in-process envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - Envelope recipients for this request 
    # + return - A successful response or an error 
    remote isolated function updateEnvelopeRecipients(string accountId, string envelopeId, EnvelopeRecipients payload, map<string|string[]> headers = {}, *UpdateEnvelopeRecipientsQueries queries) returns RecipientsUpdateSummary|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/recipients`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Adds one or more recipients to an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - Envelope recipients used to add one or more recipients to an envelope 
    # + return - A successful response or an error 
    remote isolated function createEnvelopeRecipients(string accountId, string envelopeId, EnvelopeRecipients payload, map<string|string[]> headers = {}, *CreateEnvelopeRecipientsQueries queries) returns EnvelopeRecipients|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/recipients`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes recipients from an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - Envelope recipients used to delete recipients from an envelope 
    # + return - A successful response or an error 
    remote isolated function deleteEnvelopeRecipients(string accountId, string envelopeId, EnvelopeRecipients payload, map<string|string[]> headers = {}) returns EnvelopeRecipients|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/recipients`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->delete(resourcePath, request, headers);
    }

    # Deletes a recipient from an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + recipientId - A local reference used to map
    # recipients to other objects, such as specific
    # document tabs.
    # A `recipientId` must be
    # either an integer or a GUID,
    # and the `recipientId` must be
    # unique within an envelope.
    # For example, many envelopes assign the first recipient
    # a `recipientId` of `1`
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteEnvelopeRecipient(string accountId, string envelopeId, string recipientId, map<string|string[]> headers = {}) returns EnvelopeRecipients|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/recipients/${getEncodedUri(recipientId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Gets the default Electronic Record and Signature Disclosure for an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + recipientId - A local reference used to map
    # recipients to other objects, such as specific
    # document tabs.
    # A `recipientId` must be
    # either an integer or a GUID,
    # and the `recipientId` must be
    # unique within an envelope.
    # For example, many envelopes assign the first recipient
    # a `recipientId` of `1`
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getRecipientConsumerDisclosure(string accountId, string envelopeId, string recipientId, map<string|string[]> headers = {}, *GetRecipientConsumerDisclosureQueries queries) returns ConsumerDisclosure|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/recipients/${getEncodedUri(recipientId)}/consumer_disclosure`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Gets the Electronic Record and Signature Disclosure for a specific envelope recipient.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + languageCode - (Optional) The code for the signer language version of the disclosure that you want to retrieve, as a path parameter. The following languages are supported:
    # - Arabic (`ar`)
    # - Bulgarian (`bg`)
    # - Czech (`cs`)
    # - Chinese Simplified (`zh_CN`)
    # - Chinese Traditional (`zh_TW`)
    # - Croatian (`hr`)
    # - Danish (`da`)
    # - Dutch (`nl`)
    # - English US (`en`)
    # - English UK (`en_GB`)
    # - Estonian (`et`)
    # - Farsi (`fa`)
    # - Finnish (`fi`)
    # - French (`fr`)
    # - French Canadian (`fr_CA`)
    # - German (`de`)
    # - Greek (`el`)
    # - Hebrew (`he`)
    # - Hindi (`hi`)
    # - Hungarian (`hu`)
    # - Bahasa Indonesian (`id`)
    # - Italian (`it`)
    # - Japanese (`ja`)
    # - Korean (`ko`)
    # - Latvian (`lv`)
    # - Lithuanian (`lt`)
    # - Bahasa Melayu (`ms`)
    # - Norwegian (`no`)
    # - Polish (`pl`)
    # - Portuguese (`pt`)
    # - Portuguese Brazil (`pt_BR`)
    # - Romanian (`ro`)
    # - Russian (`ru`)
    # - Serbian (`sr`)
    # - Slovak (`sk`)
    # - Slovenian (`sl`)
    # - Spanish (`es`)
    # - Spanish Latin America (`es_MX`)
    # - Swedish (`sv`)
    # - Thai (`th`)
    # - Turkish (`tr`)
    # - Ukrainian (`uk`) 
    # - Vietnamese (`vi`)
    # Additionally, you can automatically detect the browser language being used by the viewer and display the disclosure in that language by setting the value to `browser`
    # + recipientId - A local reference used to map
    # recipients to other objects, such as specific
    # document tabs.
    # A `recipientId` must be
    # either an integer or a GUID,
    # and the `recipientId` must be
    # unique within an envelope.
    # For example, many envelopes assign the first recipient
    # a `recipientId` of `1`
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getRecipientConsumerDisclosureByLang(string accountId, string envelopeId, string languageCode, string recipientId, map<string|string[]> headers = {}, *GetRecipientConsumerDisclosureByLangQueries queries) returns ConsumerDisclosure|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/recipients/${getEncodedUri(recipientId)}/consumer_disclosure/${getEncodedUri(languageCode)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns document visibility for a recipient
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + recipientId - A local reference used to map
    # recipients to other objects, such as specific
    # document tabs.
    # A `recipientId` must be
    # either an integer or a GUID,
    # and the `recipientId` must be
    # unique within an envelope.
    # For example, many envelopes assign the first recipient
    # a `recipientId` of `1`
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getRecipientDocumentVisibility(string accountId, string envelopeId, string recipientId, map<string|string[]> headers = {}) returns DocumentVisibilityList|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/recipients/${getEncodedUri(recipientId)}/document_visibility`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates document visibility for a recipient
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + recipientId - A local reference used to map
    # recipients to other objects, such as specific
    # document tabs.
    # A `recipientId` must be
    # either an integer or a GUID,
    # and the `recipientId` must be
    # unique within an envelope.
    # For example, many envelopes assign the first recipient
    # a `recipientId` of `1`
    # + headers - Headers to be sent with the request 
    # + payload - Document visibility list used to update document visibility for a recipient 
    # + return - A successful response or an error 
    remote isolated function updateRecipientDocumentVisibility(string accountId, string envelopeId, string recipientId, DocumentVisibilityList payload, map<string|string[]> headers = {}) returns DocumentVisibilityList|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/recipients/${getEncodedUri(recipientId)}/document_visibility`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Creates a resource token for a sender to request ID Evidence data. 
    #
    # + accountId - The account ID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + recipientId - The `recipientIdGuid`
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function createRecipientProofFileToken(string accountId, string envelopeId, string recipientId, map<string|string[]> headers = {}, *CreateRecipientProofFileTokenQueries queries) returns IdEvidenceResourceToken|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/recipients/${getEncodedUri(recipientId)}/identity_proof_token`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Gets the initials image for a user.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + recipientId - A local reference used to map
    # recipients to other objects, such as specific
    # document tabs.
    # A `recipientId` must be
    # either an integer or a GUID,
    # and the `recipientId` must be
    # unique within an envelope.
    # For example, many envelopes assign the first recipient
    # a `recipientId` of `1`
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getRecipientInitialsImage(string accountId, string envelopeId, string recipientId, map<string|string[]> headers = {}, *GetRecipientInitialsImageQueries queries) returns http:Response|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/recipients/${getEncodedUri(recipientId)}/initials_image`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Sets the initials image for an accountless signer.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + recipientId - A local reference used to map
    # recipients to other objects, such as specific
    # document tabs.
    # A `recipientId` must be
    # either an integer or a GUID,
    # and the `recipientId` must be
    # unique within an envelope.
    # For example, many envelopes assign the first recipient
    # a `recipientId` of `1`
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function updateRecipientInitialsImage(string accountId, string envelopeId, string recipientId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/recipients/${getEncodedUri(recipientId)}/initials_image`;
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Gets signature information for a signer or sign-in-person recipient.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + recipientId - A local reference used to map
    # recipients to other objects, such as specific
    # document tabs.
    # A `recipientId` must be
    # either an integer or a GUID,
    # and the `recipientId` must be
    # unique within an envelope.
    # For example, many envelopes assign the first recipient
    # a `recipientId` of `1`
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getRecipientSignature(string accountId, string envelopeId, string recipientId, map<string|string[]> headers = {}) returns UserSignature|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/recipients/${getEncodedUri(recipientId)}/signature`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieve signature image information for a signer/sign-in-person recipient.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + recipientId - A local reference used to map
    # recipients to other objects, such as specific
    # document tabs.
    # A `recipientId` must be
    # either an integer or a GUID,
    # and the `recipientId` must be
    # unique within an envelope.
    # For example, many envelopes assign the first recipient
    # a `recipientId` of `1`
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getRecipientSignatureImage(string accountId, string envelopeId, string recipientId, map<string|string[]> headers = {}, *GetRecipientSignatureImageQueries queries) returns http:Response|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/recipients/${getEncodedUri(recipientId)}/signature_image`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Sets the signature image for an accountless signer.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + recipientId - A local reference used to map
    # recipients to other objects, such as specific
    # document tabs.
    # A `recipientId` must be
    # either an integer or a GUID,
    # and the `recipientId` must be
    # unique within an envelope.
    # For example, many envelopes assign the first recipient
    # a `recipientId` of `1`
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function updateRecipientSignatureImage(string accountId, string envelopeId, string recipientId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/recipients/${getEncodedUri(recipientId)}/signature_image`;
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Gets the tabs information for a signer or sign-in-person recipient in an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + recipientId - A local reference used to map
    # recipients to other objects, such as specific
    # document tabs.
    # A `recipientId` must be
    # either an integer or a GUID,
    # and the `recipientId` must be
    # unique within an envelope.
    # For example, many envelopes assign the first recipient
    # a `recipientId` of `1`
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listEnvelopeRecipientTabs(string accountId, string envelopeId, string recipientId, map<string|string[]> headers = {}, *ListEnvelopeRecipientTabsQueries queries) returns EnvelopeRecipientTabs|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/recipients/${getEncodedUri(recipientId)}/tabs`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the tabs for a recipient.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + recipientId - A local reference used to map
    # recipients to other objects, such as specific
    # document tabs.
    # A `recipientId` must be
    # either an integer or a GUID,
    # and the `recipientId` must be
    # unique within an envelope.
    # For example, many envelopes assign the first recipient
    # a `recipientId` of `1`
    # + headers - Headers to be sent with the request 
    # + payload - Envelope recipient tabs used to update the tabs for a recipient 
    # + return - A successful response or an error 
    remote isolated function updateEnvelopeRecipientTabs(string accountId, string envelopeId, string recipientId, EnvelopeRecipientTabs payload, map<string|string[]> headers = {}) returns EnvelopeRecipientTabs|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/recipients/${getEncodedUri(recipientId)}/tabs`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Adds tabs for a recipient.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + recipientId - A local reference used to map
    # recipients to other objects, such as specific
    # document tabs.
    # A `recipientId` must be
    # either an integer or a GUID,
    # and the `recipientId` must be
    # unique within an envelope.
    # For example, many envelopes assign the first recipient
    # a `recipientId` of `1`
    # + headers - Headers to be sent with the request 
    # + payload - Envelope recipient tabs used to add tabs for a recipient 
    # + return - A successful response or an error 
    remote isolated function createEnvelopeRecipientTabs(string accountId, string envelopeId, string recipientId, EnvelopeRecipientTabs payload, map<string|string[]> headers = {}) returns EnvelopeRecipientTabs|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/recipients/${getEncodedUri(recipientId)}/tabs`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes the tabs associated with a recipient.
    # 
    # **Note:** It is an error to delete a tab that has the
    # `templateLocked` property set to true.
    # This property corresponds to the **Restrict changes** option in the web app.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + recipientId - A local reference used to map
    # recipients to other objects, such as specific
    # document tabs.
    # A `recipientId` must be
    # either an integer or a GUID,
    # and the `recipientId` must be
    # unique within an envelope.
    # For example, many envelopes assign the first recipient
    # a `recipientId` of `1`
    # + headers - Headers to be sent with the request 
    # + payload - Envelope recipient tabs used to delete the tabs associated with a recipient 
    # + return - A successful response or an error 
    remote isolated function deleteEnvelopeRecipientTabs(string accountId, string envelopeId, string recipientId, EnvelopeRecipientTabs payload, map<string|string[]> headers = {}) returns EnvelopeRecipientTabs|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/recipients/${getEncodedUri(recipientId)}/tabs`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->delete(resourcePath, request, headers);
    }

    # Create the link to the page for manually reviewing IDs.
    #
    # + accountId - A value that identifies your account. This value is automatically generated by Docusign for any account you create. Copy the value from the API Account ID field in the [AppsI and Keys](https://support.docusign.com/s/document-item?bundleId=pik1583277475390&topicId=pmp1583277397015.html) page
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + recipientId - A GUID value that Docusign assigns to identify each recipient in an envelope. This value is globally unique for all recipients, not just those in your account.
    # The specified recipient must belong to a workflow that allows the [manual review](https://support.docusign.com/s/document-item?bundleId=pik1583277475390&topicId=eya1583277454804.html) of IDs. In addition, the status of the automatic verification for this recipient must return `Failed` and the value of the `vendorFailureStatusCode` field must be `MANUAL_REVIEW_STARTED` as shown in the following extract of a response to the [GET ENVELOPE](/docs/esign-rest-api/reference/envelopes/envelopes/get/) method:
    # <p>
    # ```
    # "recipientAuthenticationStatus": {
    #        "identityVerificationResult": { 
    #              "status": "Failed",
    #              "eventTimestamp": "2020-09-04T16:59:42.8045667Z",
    #              "vendorFailureStatusCode": "MANUAL_REVIEW_STARTED"
    #         }
    #   }
    # ```
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function createRecipientManualReviewView(string accountId, string envelopeId, string recipientId, map<string|string[]> headers = {}) returns ViewUrl|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/recipients/${getEncodedUri(recipientId)}/views/identity_manual_review`;
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Updates document visibility for recipients
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - Document visibility list used to update document visibility for recipients 
    # + return - A successful response or an error 
    remote isolated function updateRecipientsDocumentVisibility(string accountId, string envelopeId, DocumentVisibilityList payload, map<string|string[]> headers = {}) returns DocumentVisibilityList|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/recipients/document_visibility`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Creates a preview of the responsive versions of all of the documents in an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - Document HTML definition for this request 
    # + return - A successful response or an error 
    remote isolated function createEnvelopeResponsivePreview(string accountId, string envelopeId, DocumentHtmlDefinition payload, map<string|string[]> headers = {}) returns DocumentHtmlDefinitions|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/responsive_html_preview`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Lists the users an envelope is shared with.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listEnvelopeShares(string accountId, string envelopeId, map<string|string[]> headers = {}) returns EnvelopesSharesResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/shares`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the share permissions of an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - Envelopes shares request used to update the share permissions of an envelope 
    # + return - A successful response or an error 
    remote isolated function updateEnvelopeShares(string accountId, string envelopeId, EnvelopesSharesRequest payload, map<string|string[]> headers = {}) returns EnvelopesSharesResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/shares`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Shares an envelope with one or more users.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - Envelopes shares request used to share an envelope with one or more users 
    # + return - A successful response or an error 
    remote isolated function createEnvelopeShares(string accountId, string envelopeId, EnvelopesSharesRequest payload, map<string|string[]> headers = {}) returns EnvelopesSharesResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/shares`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Removes one or more shares from an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - Envelopes shares request used to remove one or more shares from an envelope 
    # + return - A successful response or an error 
    remote isolated function deleteEnvelopeShares(string accountId, string envelopeId, EnvelopesSharesRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/shares`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->delete(resourcePath, request, headers);
    }

    # Retrieves a single envelope share.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + shareId - The ID of the envelope share.
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getEnvelopeShare(string accountId, string envelopeId, string shareId, map<string|string[]> headers = {}) returns EnvelopesShareResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/shares/${getEncodedUri(shareId)}`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the permissions of a single envelope share.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + shareId - The ID of the envelope share to update.
    # + headers - Headers to be sent with the request 
    # + payload - Envelopes share permission request used to update the permissions of a single envelope share 
    # + return - A successful response or an error 
    remote isolated function updateEnvelopeShare(string accountId, string envelopeId, string shareId, EnvelopesSharePermissionRequest payload, map<string|string[]> headers = {}) returns EnvelopesShareResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/shares/${getEncodedUri(shareId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Removes a single share from an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + shareId - The ID of the envelope share to delete.
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteEnvelopeShare(string accountId, string envelopeId, string shareId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/shares/${getEncodedUri(shareId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Reserved for Docusign.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getTabsBlob(string accountId, string envelopeId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/tabs_blob`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Reserved for Docusign.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function updateTabsBlob(string accountId, string envelopeId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/tabs_blob`;
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Gets templates used in an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listEnvelopeTemplates(string accountId, string envelopeId, map<string|string[]> headers = {}, *ListEnvelopeTemplatesQueries queries) returns TemplateInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/templates`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Adds templates to an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - Document template list used to add templates to an envelope 
    # + return - A successful response or an error 
    remote isolated function addEnvelopeTemplates(string accountId, string envelopeId, DocumentTemplateList payload, map<string|string[]> headers = {}, *AddEnvelopeTemplatesQueries queries) returns DocumentTemplateList|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/templates`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a URL to the envelope correction UI. Use after an envelope has been sent.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - Envelope view request for this request 
    # + return - A successful response or an error 
    remote isolated function createEnvelopeCorrectView(string accountId, string envelopeId, EnvelopeViewRequest payload, map<string|string[]> headers = {}) returns EnvelopeViews|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/views/correct`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Revokes the correction view URL to the Envelope UI.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - Correct view request used to revoke the correction view URL to the Envelope UI 
    # + return - A successful response or an error 
    remote isolated function deleteEnvelopeCorrectView(string accountId, string envelopeId, CorrectViewRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/views/correct`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->delete(resourcePath, request, headers);
    }

    # Returns a URL to the edit view UI. Use before an envelope has been sent.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - Envelope view request for this request 
    # + return - A successful response or an error 
    remote isolated function createEnvelopeEditView(string accountId, string envelopeId, EnvelopeViewRequest payload, map<string|string[]> headers = {}) returns EnvelopeViews|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/views/edit`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a URL to the recipient view UI. For signer recipients, returns the embedded signing view. Can also be used for other recipient types.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The ID of the draft envelope or template to preview
    # + headers - Headers to be sent with the request 
    # + payload - Recipient view request for this request 
    # + return - A successful response or an error 
    remote isolated function createEnvelopeRecipientView(string accountId, string envelopeId, RecipientViewRequest payload, map<string|string[]> headers = {}) returns EnvelopeViews|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/views/recipient`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Creates an envelope recipient preview.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - Recipient preview request used to create an envelope recipient preview 
    # + return - A successful response or an error 
    remote isolated function createEnvelopeRecipientPreview(string accountId, string envelopeId, RecipientPreviewRequest payload, map<string|string[]> headers = {}) returns ViewUrl|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/views/recipient_preview`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a URL to the sender view UI. Used before an envelope has been sent.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - Envelope view request for this request 
    # + return - A successful response or an error 
    remote isolated function createEnvelopeSenderView(string accountId, string envelopeId, EnvelopeViewRequest payload, map<string|string[]> headers = {}) returns EnvelopeViews|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/views/sender`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a URL to the shared recipient view UI for an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - Recipient view request used to return a URL to the shared recipient view UI for an envelope 
    # + return - A successful response or an error 
    remote isolated function createEnvelopeSharedView(string accountId, string envelopeId, RecipientViewRequest payload, map<string|string[]> headers = {}) returns ViewUrl|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/views/shared`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns the workflow definition for an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getEnvelopeWorkflow(string accountId, string envelopeId, map<string|string[]> headers = {}) returns Workflow|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/workflow`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the workflow definition for an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - Workflow used to update the workflow definition for an envelope 
    # + return - A successful response or an error 
    remote isolated function updateEnvelopeWorkflow(string accountId, string envelopeId, Workflow payload, map<string|string[]> headers = {}) returns Workflow|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/workflow`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete the workflow definition for an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteEnvelopeWorkflow(string accountId, string envelopeId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/workflow`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Returns the scheduled sending rules for an envelope's workflow definition.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getEnvelopeScheduledSending(string accountId, string envelopeId, map<string|string[]> headers = {}) returns ScheduledSending|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/workflow/scheduledSending`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the scheduled sending rules for an envelope's workflow.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - An object that describes the settings for scheduled sending 
    # + return - A successful response or an error 
    remote isolated function updateEnvelopeScheduledSending(string accountId, string envelopeId, ScheduledSending payload, map<string|string[]> headers = {}) returns ScheduledSending|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/workflow/scheduledSending`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes the scheduled sending rules for the envelope's workflow.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteEnvelopeScheduledSending(string accountId, string envelopeId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/workflow/scheduledSending`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Adds a new step to an envelope's workflow.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + headers - Headers to be sent with the request 
    # + payload - Workflow step used to add a new step to an envelope's workflow 
    # + return - A successful response or an error 
    remote isolated function createEnvelopeWorkflowStep(string accountId, string envelopeId, WorkflowStep payload, map<string|string[]> headers = {}) returns WorkflowStep|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/workflow/steps`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a specified workflow step for a specified template.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + workflowStepId - The ID of the workflow step
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getEnvelopeWorkflowStep(string accountId, string envelopeId, string workflowStepId, map<string|string[]> headers = {}) returns WorkflowStep|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/workflow/steps/${getEncodedUri(workflowStepId)}`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the specified workflow step for an envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + workflowStepId - The ID of the workflow step
    # + headers - Headers to be sent with the request 
    # + payload - Workflow step used to update the specified workflow step for an envelope 
    # + return - A successful response or an error 
    remote isolated function updateEnvelopeWorkflowStep(string accountId, string envelopeId, string workflowStepId, WorkflowStep payload, map<string|string[]> headers = {}) returns WorkflowStep|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/workflow/steps/${getEncodedUri(workflowStepId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes a workflow step from an envelope's workflow definition.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + workflowStepId - The ID of the workflow step
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteEnvelopeWorkflowStep(string accountId, string envelopeId, string workflowStepId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/workflow/steps/${getEncodedUri(workflowStepId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Returns the delayed routing rules for an envelope's workflow step definition.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + workflowStepId - The ID of the workflow step
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getEnvelopeDelayedRouting(string accountId, string envelopeId, string workflowStepId, map<string|string[]> headers = {}) returns DelayedRouting|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/workflow/steps/${getEncodedUri(workflowStepId)}/delayedRouting`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the delayed routing rules for an envelope's workflow step definition.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + workflowStepId - The ID of the workflow step
    # + headers - Headers to be sent with the request 
    # + payload - A complex element that specifies the delayed routing settings for the workflow step 
    # + return - A successful response or an error 
    remote isolated function updateEnvelopeDelayedRouting(string accountId, string envelopeId, string workflowStepId, DelayedRouting payload, map<string|string[]> headers = {}) returns DelayedRouting|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/workflow/steps/${getEncodedUri(workflowStepId)}/delayedRouting`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes the delayed routing rules for the specified envelope workflow step.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeId - The envelope's GUID. 
    # Example: `93be49ab-xxxx-xxxx-xxxx-f752070d71ec`
    # + workflowStepId - The ID of the workflow step
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteEnvelopeDelayedRouting(string accountId, string envelopeId, string workflowStepId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/${getEncodedUri(envelopeId)}/workflow/steps/${getEncodedUri(workflowStepId)}/delayedRouting`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Lists the envelopes shared with the current user.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listSharedEnvelopes(string accountId, map<string|string[]> headers = {}, *ListSharedEnvelopesQueries queries) returns EnvelopesInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/shared`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Gets envelope statuses for a set of envelopes.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - Envelope IDs request used to get envelope statuses for a set of envelopes 
    # + return - A successful response or an error 
    remote isolated function listEnvelopeStatuses(string accountId, EnvelopeIdsRequest payload, map<string|string[]> headers = {}, *ListEnvelopeStatusesQueries queries) returns EnvelopesInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/status`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Gets envelope transfer rules.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listEnvelopeTransferRules(string accountId, map<string|string[]> headers = {}, *ListEnvelopeTransferRulesQueries queries) returns EnvelopeTransferRuleInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/transfer_rules`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Changes the status of multiple envelope transfer rules.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Envelope transfer rule information used to change the status of multiple envelope transfer rules 
    # + return - A successful response or an error 
    remote isolated function updateEnvelopeTransferRules(string accountId, EnvelopeTransferRuleInformation payload, map<string|string[]> headers = {}) returns EnvelopeTransferRuleInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/transfer_rules`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Creates an envelope transfer rule.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Envelope transfer rule request used to create an envelope transfer rule 
    # + return - A successful response or an error 
    remote isolated function createEnvelopeTransferRules(string accountId, EnvelopeTransferRuleRequest payload, map<string|string[]> headers = {}) returns EnvelopeTransferRuleInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/transfer_rules`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Changes the status of an envelope transfer rule.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeTransferRuleId - The ID of the envelope transfer rule. The system generates this ID when the rule is first created
    # + headers - Headers to be sent with the request 
    # + payload - Envelope transfer rule used to change the status of an envelope transfer rule 
    # + return - A successful response or an error 
    remote isolated function updateEnvelopeTransferRule(string accountId, string envelopeTransferRuleId, EnvelopeTransferRule payload, map<string|string[]> headers = {}) returns EnvelopeTransferRule|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/transfer_rules/${getEncodedUri(envelopeTransferRuleId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes an envelope transfer rule.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + envelopeTransferRuleId - The ID of the envelope transfer rule. The system generates this ID when the rule is first created
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteEnvelopeTransferRule(string accountId, string envelopeTransferRuleId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/envelopes/transfer_rules/${getEncodedUri(envelopeTransferRuleId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Retrieves the list of favorite templates for the account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listFavoriteTemplates(string accountId, map<string|string[]> headers = {}) returns FavoriteTemplatesInfo|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/favorite_templates`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Set one or more templates as account favorites.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Favorite templates info used to set one or more templates as account favorites 
    # + return - A successful response or an error 
    remote isolated function addFavoriteTemplates(string accountId, FavoriteTemplatesInfo payload, map<string|string[]> headers = {}) returns FavoriteTemplatesInfo|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/favorite_templates`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Remove one or more templates from the account favorites.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Favorite templates info used to remove one or more templates from the account favorites 
    # + return - A successful response or an error 
    remote isolated function removeFavoriteTemplates(string accountId, FavoriteTemplatesInfo payload, map<string|string[]> headers = {}) returns FavoriteTemplatesInfo|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/favorite_templates`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->delete(resourcePath, request, headers);
    }

    # Returns a list of the account's folders.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listFolders(string accountId, map<string|string[]> headers = {}, *ListFoldersQueries queries) returns FoldersResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/folders`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Gets information about items in a specified folder. 
    #
    # + accountId - The external account number (int) or account ID GUID
    # + folderId - The ID of the folder
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listFolderItems(string accountId, string folderId, map<string|string[]> headers = {}, *ListFolderItemsQueries queries) returns FolderItemsResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/folders/${getEncodedUri(folderId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Moves a set of envelopes from their current folder to another folder.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + folderId - The ID of the folder
    # + headers - Headers to be sent with the request 
    # + payload - Folders request used to move a set of envelopes from their current folder to another folder 
    # + return - A successful response or an error 
    remote isolated function moveEnvelopesToFolder(string accountId, string folderId, FoldersRequest payload, map<string|string[]> headers = {}) returns FoldersResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/folders/${getEncodedUri(folderId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Gets information about groups associated with the account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listGroups(string accountId, map<string|string[]> headers = {}, *ListGroupsQueries queries) returns GroupInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/groups`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the group information for a group.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Group information used to update the group information for a group 
    # + return - A successful response or an error 
    remote isolated function updateGroups(string accountId, GroupInformation payload, map<string|string[]> headers = {}) returns GroupInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/groups`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Creates one or more groups for the account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Group information used to create one or more groups for the account 
    # + return - A successful response or an error 
    remote isolated function createGroups(string accountId, GroupInformation payload, map<string|string[]> headers = {}) returns GroupInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/groups`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes an existing user group.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Group information used to delete an existing user group 
    # + return - A successful response or an error 
    remote isolated function deleteGroups(string accountId, GroupInformation payload, map<string|string[]> headers = {}) returns GroupInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/groups`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->delete(resourcePath, request, headers);
    }

    # Gets the brand information for a group.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + groupId - The ID of the group
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listGroupBrands(string accountId, string groupId, map<string|string[]> headers = {}) returns GroupBrands|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/groups/${getEncodedUri(groupId)}/brands`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Adds an existing brand to a group.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + groupId - The ID of the group being accessed
    # + headers - Headers to be sent with the request 
    # + payload - Brands request used to add an existing brand to a group 
    # + return - A successful response or an error 
    remote isolated function addGroupBrands(string accountId, string groupId, BrandsRequest payload, map<string|string[]> headers = {}) returns GroupBrands|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/groups/${getEncodedUri(groupId)}/brands`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes brand information from a group.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + groupId - The ID of the group
    # + headers - Headers to be sent with the request 
    # + payload - Brands request used to delete brand information from a group 
    # + return - A successful response or an error 
    remote isolated function deleteGroupBrands(string accountId, string groupId, BrandsRequest payload, map<string|string[]> headers = {}) returns GroupBrands|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/groups/${getEncodedUri(groupId)}/brands`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->delete(resourcePath, request, headers);
    }

    # Gets a list of users in a group.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + groupId - The ID of the group being accessed
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listGroupUsers(string accountId, string groupId, map<string|string[]> headers = {}, *ListGroupUsersQueries queries) returns GroupUsersResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/groups/${getEncodedUri(groupId)}/users`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Adds one or more users to an existing group.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + groupId - The ID of the group being accessed
    # + headers - Headers to be sent with the request 
    # + payload - User info list used to add one or more users to an existing group 
    # + return - A successful response or an error 
    remote isolated function addGroupUsers(string accountId, string groupId, UserInfoList payload, map<string|string[]> headers = {}) returns UsersResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/groups/${getEncodedUri(groupId)}/users`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes one or more users from a group
    #
    # + accountId - The external account number (int) or account ID GUID
    # + groupId - The ID of the group being accessed
    # + headers - Headers to be sent with the request 
    # + payload - User info list used to delete one or more users from a group 
    # + return - A successful response or an error 
    remote isolated function deleteGroupUsers(string accountId, string groupId, UserInfoList payload, map<string|string[]> headers = {}) returns UsersResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/groups/${getEncodedUri(groupId)}/users`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->delete(resourcePath, request, headers);
    }

    # Retrieves the Identity Verification workflows available to an account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getAccountIdentityVerification(string accountId, map<string|string[]> headers = {}, *GetAccountIdentityVerificationQueries queries) returns AccountIdentityVerificationResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/identity_verification`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # List payment gateway accounts
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listPaymentGatewayAccounts(string accountId, map<string|string[]> headers = {}) returns PaymentGatewayAccountsInfo|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/payment_gateway_accounts`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Gets a list of permission profiles.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listPermissionProfiles(string accountId, map<string|string[]> headers = {}, *ListPermissionProfilesQueries queries) returns PermissionProfileInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/permission_profiles`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a new permission profile for an account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - Permission profile used to create a new permission profile for an account 
    # + return - A successful response or an error 
    remote isolated function createPermissionProfile(string accountId, PermissionProfile payload, map<string|string[]> headers = {}, *CreatePermissionProfileQueries queries) returns PermissionProfile|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/permission_profiles`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a permission profile for an account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + permissionProfileId - The ID of the permission profile.
    # Use [AccountPermissionProfiles: list](/docs/esign-rest-api/reference/accounts/accountpermissionprofiles/list/)
    # to get a list of permission profiles and their IDs.
    # You can also download a CSV file of all permission profiles
    # and their IDs from the **Settings > Permission Profiles** page
    # of your eSignature account page
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getPermissionProfile(string accountId, string permissionProfileId, map<string|string[]> headers = {}, *GetPermissionProfileQueries queries) returns PermissionProfile|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/permission_profiles/${getEncodedUri(permissionProfileId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates a permission profile.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + permissionProfileId - The ID of the permission profile.
    # Use [AccountPermissionProfiles: list](/docs/esign-rest-api/reference/accounts/accountpermissionprofiles/list/)
    # to get a list of permission profiles and their IDs.
    # You can also download a CSV file of all permission profiles
    # and their IDs from the **Settings > Permission Profiles** page
    # of your eSignature account page
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - Permission profile used to update a permission profile 
    # + return - A successful response or an error 
    remote isolated function updatePermissionProfile(string accountId, string permissionProfileId, PermissionProfile payload, map<string|string[]> headers = {}, *UpdatePermissionProfileQueries queries) returns PermissionProfile|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/permission_profiles/${getEncodedUri(permissionProfileId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes a permission profile from an account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + permissionProfileId - The ID of the permission profile.
    # Use [AccountPermissionProfiles: list](/docs/esign-rest-api/reference/accounts/accountpermissionprofiles/list/)
    # to get a list of permission profiles and their IDs.
    # You can also download a CSV file of all permission profiles
    # and their IDs from the **Settings > Permission Profiles** page
    # of your eSignature account page
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deletePermissionProfile(string accountId, string permissionProfileId, map<string|string[]> headers = {}, *DeletePermissionProfileQueries queries) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/permission_profiles/${getEncodedUri(permissionProfileId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Returns a list of PowerForms.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listPowerForms(string accountId, map<string|string[]> headers = {}, *ListPowerFormsQueries queries) returns PowerFormsResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/powerforms`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a new PowerForm
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Information about any PowerForms that are included in the envelope 
    # + return - A successful response or an error 
    remote isolated function createPowerForm(string accountId, PowerForm payload, map<string|string[]> headers = {}) returns PowerForm|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/powerforms`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes one or more PowerForms.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Power forms request used to delete one or more PowerForms 
    # + return - A successful response or an error 
    remote isolated function deletePowerForms(string accountId, PowerFormsRequest payload, map<string|string[]> headers = {}) returns PowerFormsResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/powerforms`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->delete(resourcePath, request, headers);
    }

    # Returns a single PowerForm.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + powerFormId - The ID of the PowerForm
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getPowerForm(string accountId, string powerFormId, map<string|string[]> headers = {}) returns PowerForm|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/powerforms/${getEncodedUri(powerFormId)}`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates an existing PowerForm.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + powerFormId - The ID of the PowerForm
    # + headers - Headers to be sent with the request 
    # + payload - Information about any PowerForms that are included in the envelope 
    # + return - A successful response or an error 
    remote isolated function updatePowerForm(string accountId, string powerFormId, PowerForm payload, map<string|string[]> headers = {}) returns PowerForm|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/powerforms/${getEncodedUri(powerFormId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes a PowerForm.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + powerFormId - The ID of the PowerForm
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deletePowerForm(string accountId, string powerFormId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/powerforms/${getEncodedUri(powerFormId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Returns the data that users entered in a PowerForm.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + powerFormId - The ID of the PowerForm
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getPowerFormFormData(string accountId, string powerFormId, map<string|string[]> headers = {}, *GetPowerFormFormDataQueries queries) returns PowerFormsFormDataResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/powerforms/${getEncodedUri(powerFormId)}/form_data`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Gets the recipient names associated with an email address.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listRecipientNames(string accountId, map<string|string[]> headers = {}, *ListRecipientNamesQueries queries) returns RecipientNamesResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/recipient_names`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Gets PowerForm senders.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listPowerFormSenders(string accountId, map<string|string[]> headers = {}, *ListPowerFormSendersQueries queries) returns PowerFormSendersResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/powerforms/senders`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns available seals for specified account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listSealProviders(string accountId, map<string|string[]> headers = {}) returns AccountSealProviders|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/seals`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Deprecated. Use Envelopes: listStatusChanges.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + searchFolderId - Specifies the envelope group that is searched by the request. These are logical groupings, not actual folder names. Valid values are: drafts, awaiting_my_signature, completed, out_for_signature
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getSearchFolderContents(string accountId, string searchFolderId, map<string|string[]> headers = {}, *GetSearchFolderContentsQueries queries) returns FolderItemResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/search_folders/${getEncodedUri(searchFolderId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Gets account settings information.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getAccountSettings(string accountId, map<string|string[]> headers = {}) returns AccountSettingsInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/settings`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the account settings for an account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Account settings information used to update the account settings for an account 
    # + return - A successful response or an error 
    remote isolated function updateAccountSettings(string accountId, AccountSettingsInformation payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/settings`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Gets the BCC email archive configurations for an account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listBccEmailArchives(string accountId, map<string|string[]> headers = {}, *ListBccEmailArchivesQueries queries) returns BccEmailArchiveList|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/settings/bcc_email_archives`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a BCC email archive configuration.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Boolean that specifies whether BCC for Email Archive is enabled for the account. BCC for Email Archive allows you to set up an archive email address so that a BCC copy of an envelope is sent only to that address 
    # + return - A successful response or an error 
    remote isolated function createBccEmailArchive(string accountId, BccEmailArchive payload, map<string|string[]> headers = {}) returns BccEmailArchive|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/settings/bcc_email_archives`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Gets a BCC email archive configuration and its history.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + bccEmailArchiveId - The ID of the BCC email archive configuration
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listBccEmailArchiveHistory(string accountId, string bccEmailArchiveId, map<string|string[]> headers = {}, *ListBccEmailArchiveHistoryQueries queries) returns BccEmailArchiveHistoryList|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/settings/bcc_email_archives/${getEncodedUri(bccEmailArchiveId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a BCC email archive configuration.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + bccEmailArchiveId - The ID of the BCC email archive configuration
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteBccEmailArchive(string accountId, string bccEmailArchiveId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/settings/bcc_email_archives/${getEncodedUri(bccEmailArchiveId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Returns the configuration information for the eNote eOriginal integration.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getENoteConfiguration(string accountId, map<string|string[]> headers = {}) returns ENoteConfiguration|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/settings/enote_configuration`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates configuration information for the eNote eOriginal integration.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - E note configuration used to update configuration information for the eNote eOriginal integration 
    # + return - A successful response or an error 
    remote isolated function updateENoteConfiguration(string accountId, ENoteConfiguration payload, map<string|string[]> headers = {}) returns ENoteConfiguration|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/settings/enote_configuration`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes configuration information for the eNote eOriginal integration.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteENoteConfiguration(string accountId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/settings/enote_configuration`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Gets the envelope purge configuration for an account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getEnvelopePurgeConfiguration(string accountId, map<string|string[]> headers = {}) returns EnvelopePurgeConfiguration|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/settings/envelope_purge_configuration`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Sets the envelope purge configuration for an account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Envelope purge configuration used to set the envelope purge configuration for an account 
    # + return - A successful response or an error 
    remote isolated function updateEnvelopePurgeConfiguration(string accountId, EnvelopePurgeConfiguration payload, map<string|string[]> headers = {}) returns EnvelopePurgeConfiguration|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/settings/envelope_purge_configuration`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Gets envelope notification defaults.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getNotificationDefaults(string accountId, map<string|string[]> headers = {}) returns NotificationDefaults|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/settings/notification_defaults`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates envelope notification default settings.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Notification defaults used to update envelope notification default settings 
    # + return - A successful response or an error 
    remote isolated function updateNotificationDefaults(string accountId, NotificationDefaults payload, map<string|string[]> headers = {}) returns NotificationDefaults|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/settings/notification_defaults`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Gets the password rules for an account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getAccountPasswordRules(string accountId, map<string|string[]> headers = {}) returns AccountPasswordRules|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/settings/password_rules`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the password rules for an account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Account password rules used to update the password rules for an account 
    # + return - A successful response or an error 
    remote isolated function updateAccountPasswordRules(string accountId, AccountPasswordRules payload, map<string|string[]> headers = {}) returns AccountPasswordRules|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/settings/password_rules`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Returns tab settings list for specified account
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getTabSettings(string accountId, map<string|string[]> headers = {}) returns TabAccountSettings|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/settings/tabs`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Modifies tab settings for specified account
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Account-wide tab settings 
    # + return - A successful response or an error 
    remote isolated function updateTabSettings(string accountId, TabAccountSettings payload, map<string|string[]> headers = {}) returns TabAccountSettings|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/settings/tabs`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Reserved: Gets the shared item status for one or more users.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getSharedAccess(string accountId, map<string|string[]> headers = {}, *GetSharedAccessQueries queries) returns AccountSharedAccess|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/shared_access`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Reserved: Sets the shared access information for users.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - Account shared access used to set the shared access information for users 
    # + return - A successful response or an error 
    remote isolated function updateSharedAccess(string accountId, AccountSharedAccess payload, map<string|string[]> headers = {}, *UpdateSharedAccessQueries queries) returns AccountSharedAccess|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/shared_access`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Gets the available signature providers for an account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listSignatureProviders(string accountId, map<string|string[]> headers = {}) returns AccountSignatureProviders|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/signatureProviders`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a list of stamps available in the account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listAccountSignatures(string accountId, map<string|string[]> headers = {}, *ListAccountSignaturesQueries queries) returns AccountSignaturesInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/signatures`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates an account stamp.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Account signatures information used to update an account stamp 
    # + return - A successful response or an error 
    remote isolated function updateAccountSignatures(string accountId, AccountSignaturesInformation payload, map<string|string[]> headers = {}) returns AccountSignaturesInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/signatures`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Adds or updates one or more account stamps.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - Account signatures information used to add or updates one or more account stamps 
    # + return - A successful response or an error 
    remote isolated function createAccountSignatures(string accountId, AccountSignaturesInformation payload, map<string|string[]> headers = {}, *CreateAccountSignaturesQueries queries) returns AccountSignaturesInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/signatures`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns information about the specified stamp.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + signatureId - The ID of the account stamp
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getAccountSignature(string accountId, string signatureId, map<string|string[]> headers = {}) returns AccountSignature|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/signatures/${getEncodedUri(signatureId)}`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates an account stamp by ID.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + signatureId - The ID of the account stamp
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - Account signature definition used to update an account stamp by ID 
    # + return - A successful response or an error 
    remote isolated function updateAccountSignature(string accountId, string signatureId, AccountSignatureDefinition payload, map<string|string[]> headers = {}, *UpdateAccountSignatureQueries queries) returns AccountSignature|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/signatures/${getEncodedUri(signatureId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes an account stamp.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + signatureId - The ID of the account stamp
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteAccountSignature(string accountId, string signatureId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/signatures/${getEncodedUri(signatureId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Returns the image for an account stamp.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + imageType - Specificies the type of image. Valid values:
    # - `stamp_image`
    # - `signature_image`
    # - `initials_image`
    # + signatureId - The ID of the account stamp
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getAccountSignatureImage(string accountId, string imageType, string signatureId, map<string|string[]> headers = {}, *GetAccountSignatureImageQueries queries) returns http:Response|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/signatures/${getEncodedUri(signatureId)}/${getEncodedUri(imageType)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Sets a signature image, initials, or stamp.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + imageType - Specificies the type of image. Valid values:
    # - `stamp_image`
    # - `signature_image`
    # - `initials_image`
    # + signatureId - The ID of the account stamp
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function updateAccountSignatureImage(string accountId, string imageType, string signatureId, map<string|string[]> headers = {}, *UpdateAccountSignatureImageQueries queries) returns AccountSignature|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/signatures/${getEncodedUri(signatureId)}/${getEncodedUri(imageType)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes the image for a stamp.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + imageType - Specificies the type of image. Valid values:
    # - `stamp_image`
    # - `signature_image`
    # - `initials_image`
    # + signatureId - The ID of the account stamp
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteAccountSignatureImage(string accountId, string imageType, string signatureId, map<string|string[]> headers = {}) returns AccountSignature|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/signatures/${getEncodedUri(signatureId)}/${getEncodedUri(imageType)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Gets a list of the Signing Groups in an account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listSigningGroups(string accountId, map<string|string[]> headers = {}, *ListSigningGroupsQueries queries) returns SigningGroupInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/signing_groups`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates signing group names.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Signing group information used to update signing group names 
    # + return - A successful response or an error 
    remote isolated function updateSigningGroups(string accountId, SigningGroupInformation payload, map<string|string[]> headers = {}) returns SigningGroupInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/signing_groups`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Creates a signing group. 
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Signing group information used to create a signing group 
    # + return - A successful response or an error 
    remote isolated function createSigningGroups(string accountId, SigningGroupInformation payload, map<string|string[]> headers = {}) returns SigningGroupInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/signing_groups`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes one or more signing groups.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Signing group information used to delete one or more signing groups 
    # + return - A successful response or an error 
    remote isolated function deleteSigningGroups(string accountId, SigningGroupInformation payload, map<string|string[]> headers = {}) returns SigningGroupInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/signing_groups`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->delete(resourcePath, request, headers);
    }

    # Gets information about a signing group. 
    #
    # + accountId - The external account number (int) or account ID GUID
    # + signingGroupId - The ID of the [signing group](https://support.docusign.com/s/document-item?bundleId=gav1643676262430&topicId=zgn1578456447934.html)
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getSigningGroup(string accountId, string signingGroupId, map<string|string[]> headers = {}) returns SigningGroup|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/signing_groups/${getEncodedUri(signingGroupId)}`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates a signing group. 
    #
    # + accountId - The external account number (int) or account ID GUID
    # + signingGroupId - The ID of the [signing group](https://support.docusign.com/s/document-item?bundleId=gav1643676262430&topicId=zgn1578456447934.html)
    # + headers - Headers to be sent with the request 
    # + payload - Signing group used to update a signing group 
    # + return - A successful response or an error 
    remote isolated function updateSigningGroup(string accountId, string signingGroupId, SigningGroup payload, map<string|string[]> headers = {}) returns SigningGroup|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/signing_groups/${getEncodedUri(signingGroupId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Gets a list of members in a Signing Group.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + signingGroupId - The ID of the [signing group](https://support.docusign.com/s/document-item?bundleId=gav1643676262430&topicId=zgn1578456447934.html).
    # **Note:** When you send an envelope to a signing group,
    # anyone in the group can open it and sign it with their own signature.
    # For this reason, Docusign recommends that
    # you do not include non-signer recipients
    # (such as carbon copy recipients)
    # in the same signing group as signer recipients.
    # However, you could create a second signing group
    # for the non-signer recipients and change t
    # he default action of Needs to Sign to a different value,
    # such as Receives a Copy
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listSigningGroupUsers(string accountId, string signingGroupId, map<string|string[]> headers = {}) returns SigningGroupUsers|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/signing_groups/${getEncodedUri(signingGroupId)}/users`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Adds members to a signing group. 
    #
    # + accountId - The external account number (int) or account ID GUID
    # + signingGroupId - The ID of the [signing group](https://support.docusign.com/s/document-item?bundleId=gav1643676262430&topicId=zgn1578456447934.html).
    # **Note:** When you send an envelope to a signing group,
    # anyone in the group can open it and sign it with their own signature.
    # For this reason, Docusign recommends that
    # you do not include non-signer recipients
    # (such as carbon copy recipients)
    # in the same signing group as signer recipients.
    # However, you could create a second signing group
    # for the non-signer recipients and change t
    # he default action of Needs to Sign to a different value,
    # such as Receives a Copy
    # + headers - Headers to be sent with the request 
    # + payload - A complex type that contains information about users in the signing group 
    # + return - A successful response or an error 
    remote isolated function addSigningGroupUsers(string accountId, string signingGroupId, SigningGroupUsers payload, map<string|string[]> headers = {}) returns SigningGroupUsers|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/signing_groups/${getEncodedUri(signingGroupId)}/users`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes  one or more members from a signing group.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + signingGroupId - The ID of the [signing group](https://support.docusign.com/s/document-item?bundleId=gav1643676262430&topicId=zgn1578456447934.html).
    # **Note:** When you send an envelope to a signing group,
    # anyone in the group can open it and sign it with their own signature.
    # For this reason, Docusign recommends that
    # you do not include non-signer recipients
    # (such as carbon copy recipients)
    # in the same signing group as signer recipients.
    # However, you could create a second signing group
    # for the non-signer recipients and change t
    # he default action of Needs to Sign to a different value,
    # such as Receives a Copy
    # + headers - Headers to be sent with the request 
    # + payload - A complex type that contains information about users in the signing group 
    # + return - A successful response or an error 
    remote isolated function deleteSigningGroupUsers(string accountId, string signingGroupId, SigningGroupUsers payload, map<string|string[]> headers = {}) returns SigningGroupUsers|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/signing_groups/${getEncodedUri(signingGroupId)}/users`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->delete(resourcePath, request, headers);
    }

    # Gets the supported languages for envelope recipients.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listSupportedLanguages(string accountId, map<string|string[]> headers = {}) returns SupportedLanguages|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/supported_languages`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Gets a list of all account tabs.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listCustomTabs(string accountId, map<string|string[]> headers = {}, *ListCustomTabsQueries queries) returns TabMetadataList|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/tab_definitions`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a custom tab.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Tab metadata used to create a custom tab 
    # + return - A successful response or an error 
    remote isolated function createCustomTab(string accountId, TabMetadata payload, map<string|string[]> headers = {}) returns TabMetadata|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/tab_definitions`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Gets custom tab information.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + customTabId - The Docusign-generated custom tab ID for the custom tab to be applied. This can only be used when adding new tabs for a recipient. When used, the new tab inherits all the custom tab properties
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getCustomTab(string accountId, string customTabId, map<string|string[]> headers = {}) returns TabMetadata|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/tab_definitions/${getEncodedUri(customTabId)}`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates custom tab information.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + customTabId - The Docusign-generated custom tab ID for the custom tab to be applied. This can only be used when adding new tabs for a recipient. When used, the new tab inherits all the custom tab properties
    # + headers - Headers to be sent with the request 
    # + payload - Tab metadata used to update custom tab information 
    # + return - A successful response or an error 
    remote isolated function updateCustomTab(string accountId, string customTabId, TabMetadata payload, map<string|string[]> headers = {}) returns TabMetadata|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/tab_definitions/${getEncodedUri(customTabId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes custom tab information.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + customTabId - The Docusign-generated custom tab ID for the custom tab to be applied. This can only be used when adding new tabs for a recipient. When used, the new tab inherits all the custom tab properties
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteCustomTab(string accountId, string customTabId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/tab_definitions/${getEncodedUri(customTabId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Gets the list of templates.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listTemplates(string accountId, map<string|string[]> headers = {}, *ListTemplatesQueries queries) returns EnvelopeTemplateResults|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the auto-match setting for multiple templates.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Template auto match list used to update the auto-match setting for multiple templates 
    # + return - A successful response or an error 
    remote isolated function updateTemplatesAutoMatch(string accountId, TemplateAutoMatchList payload, map<string|string[]> headers = {}) returns TemplateAutoMatchList|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Creates one or more templates.
    #
    # + accountId - (Required) The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Envelope template used to create one or more templates 
    # + return - A successful response or an error 
    remote isolated function createTemplate(string accountId, EnvelopeTemplate payload, map<string|string[]> headers = {}) returns TemplateSummary|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Gets a specific template associated with a specified account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getTemplate(string accountId, string templateId, map<string|string[]> headers = {}, *GetTemplateQueries queries) returns EnvelopeTemplate|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates an existing template.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + payload - Envelope template used to update an existing template 
    # + return - A successful response or an error 
    remote isolated function updateTemplate(string accountId, string templateId, EnvelopeTemplate payload, map<string|string[]> headers = {}) returns TemplateUpdateSummary|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Shares a template with a group.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + templatePart - Currently, the only defined part is **groups.**
    # + headers - Headers to be sent with the request 
    # + payload - Group information used to share a template with a group 
    # + return - A successful response or an error 
    remote isolated function shareTemplateWithGroup(string accountId, string templateId, string templatePart, GroupInformation payload, map<string|string[]> headers = {}) returns GroupInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/${getEncodedUri(templatePart)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Removes a member group's sharing permissions for a template.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + templatePart - Currently, the only defined part is **groups.**
    # + headers - Headers to be sent with the request 
    # + payload - Group information used to remove a member group's sharing permissions for a template 
    # + return - A successful response or an error 
    remote isolated function unshareTemplateWithGroup(string accountId, string templateId, string templatePart, GroupInformation payload, map<string|string[]> headers = {}) returns GroupInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/${getEncodedUri(templatePart)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->delete(resourcePath, request, headers);
    }

    # Gets the custom document fields from a template.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listTemplateCustomFields(string accountId, string templateId, map<string|string[]> headers = {}) returns CustomFields|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/custom_fields`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates envelope custom fields in a template.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + payload - Template custom fields used to update envelope custom fields in a template 
    # + return - A successful response or an error 
    remote isolated function updateTemplateCustomFields(string accountId, string templateId, TemplateCustomFields payload, map<string|string[]> headers = {}) returns CustomFields|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/custom_fields`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Creates custom document fields in an existing template document.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + payload - Template custom fields used to create custom document fields in an existing template document 
    # + return - A successful response or an error 
    remote isolated function createTemplateCustomFields(string accountId, string templateId, TemplateCustomFields payload, map<string|string[]> headers = {}) returns CustomFields|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/custom_fields`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes envelope custom fields in a template.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + payload - Template custom fields used to delete envelope custom fields in a template 
    # + return - A successful response or an error 
    remote isolated function deleteTemplateCustomFields(string accountId, string templateId, TemplateCustomFields payload, map<string|string[]> headers = {}) returns CustomFields|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/custom_fields`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->delete(resourcePath, request, headers);
    }

    # Gets a list of documents associated with a template.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listTemplateDocuments(string accountId, string templateId, map<string|string[]> headers = {}, *ListTemplateDocumentsQueries queries) returns TemplateDocumentsResult|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/documents`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Adds documents to a template document.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + payload - Envelope definition used to add documents to a template document 
    # + return - A successful response or an error 
    remote isolated function addTemplateDocuments(string accountId, string templateId, EnvelopeDefinition payload, map<string|string[]> headers = {}) returns TemplateDocumentsResult|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/documents`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes documents from a template.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + payload - Envelope definition used to delete documents from a template 
    # + return - A successful response or an error 
    remote isolated function deleteTemplateDocuments(string accountId, string templateId, EnvelopeDefinition payload, map<string|string[]> headers = {}) returns TemplateDocumentsResult|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/documents`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->delete(resourcePath, request, headers);
    }

    # Gets PDF documents from a template.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The unique ID of the document within the envelope.
    # Unlike other IDs in the eSignature API,
    # you specify the `documentId` yourself.
    # Typically the first document has the ID
    # `1`, the second document `2`, and so on,
    # but you can use any numbering scheme
    # that fits within a 32-bit signed integer
    # (1 through 2147483647).
    # Tab objects have a `documentId` property
    # that specifies the document on which to place
    # the tab
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getTemplateDocument(string accountId, string documentId, string templateId, map<string|string[]> headers = {}, *GetTemplateDocumentQueries queries) returns http:Response|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/documents/${getEncodedUri(documentId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates a template document.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The unique ID of the document within the envelope.
    # Unlike other IDs in the eSignature API,
    # you specify the `documentId` yourself.
    # Typically the first document has the ID
    # `1`, the second document `2`, and so on,
    # but you can use any numbering scheme
    # that fits within a 32-bit signed integer
    # (1 through 2147483647).
    # Tab objects have a `documentId` property
    # that specifies the document on which to place
    # the tab
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - Envelope definition used to update a template document 
    # + return - A successful response or an error 
    remote isolated function updateTemplateDocument(string accountId, string documentId, string templateId, EnvelopeDefinition payload, map<string|string[]> headers = {}, *UpdateTemplateDocumentQueries queries) returns EnvelopeDocument|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/documents/${getEncodedUri(documentId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Gets the custom document fields for a an existing template document.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The unique ID of the document within the envelope.
    # Unlike other IDs in the eSignature API,
    # you specify the `documentId` yourself.
    # Typically the first document has the ID
    # `1`, the second document `2`, and so on,
    # but you can use any numbering scheme
    # that fits within a 32-bit signed integer
    # (1 through 2147483647).
    # Tab objects have a `documentId` property
    # that specifies the document on which to place
    # the tab
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listTemplateDocumentFields(string accountId, string documentId, string templateId, map<string|string[]> headers = {}) returns DocumentFieldsInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/documents/${getEncodedUri(documentId)}/fields`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates existing custom document fields in an existing template document.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The unique ID of the document within the envelope.
    # Unlike other IDs in the eSignature API,
    # you specify the `documentId` yourself.
    # Typically the first document has the ID
    # `1`, the second document `2`, and so on,
    # but you can use any numbering scheme
    # that fits within a 32-bit signed integer
    # (1 through 2147483647).
    # Tab objects have a `documentId` property
    # that specifies the document on which to place
    # the tab
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + payload - Document fields information for this request 
    # + return - A successful response or an error 
    remote isolated function updateTemplateDocumentFields(string accountId, string documentId, string templateId, DocumentFieldsInformation payload, map<string|string[]> headers = {}) returns DocumentFieldsInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/documents/${getEncodedUri(documentId)}/fields`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Creates custom document fields in an existing template document.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The unique ID of the document within the envelope.
    # Unlike other IDs in the eSignature API,
    # you specify the `documentId` yourself.
    # Typically the first document has the ID
    # `1`, the second document `2`, and so on,
    # but you can use any numbering scheme
    # that fits within a 32-bit signed integer
    # (1 through 2147483647).
    # Tab objects have a `documentId` property
    # that specifies the document on which to place
    # the tab
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + payload - Document fields information used to create custom document fields in an existing template document 
    # + return - A successful response or an error 
    remote isolated function createTemplateDocumentFields(string accountId, string documentId, string templateId, DocumentFieldsInformation payload, map<string|string[]> headers = {}) returns DocumentFieldsInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/documents/${getEncodedUri(documentId)}/fields`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes custom document fields from an existing template document.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The unique ID of the document within the envelope.
    # Unlike other IDs in the eSignature API,
    # you specify the `documentId` yourself.
    # Typically the first document has the ID
    # `1`, the second document `2`, and so on,
    # but you can use any numbering scheme
    # that fits within a 32-bit signed integer
    # (1 through 2147483647).
    # Tab objects have a `documentId` property
    # that specifies the document on which to place
    # the tab
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + payload - Document fields information for this request 
    # + return - A successful response or an error 
    remote isolated function deleteTemplateDocumentFields(string accountId, string documentId, string templateId, DocumentFieldsInformation payload, map<string|string[]> headers = {}) returns DocumentFieldsInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/documents/${getEncodedUri(documentId)}/fields`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->delete(resourcePath, request, headers);
    }

    # Gets the Original HTML Definition used to generate the Responsive HTML for a given document in a template.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The unique ID of the document within the envelope.
    # Unlike other IDs in the eSignature API,
    # you specify the `documentId` yourself.
    # Typically the first document has the ID
    # `1`, the second document `2`, and so on,
    # but you can use any numbering scheme
    # that fits within a 32-bit signed integer
    # (1 through 2147483647).
    # Tab objects have a `documentId` property
    # that specifies the document on which to place
    # the tab
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listTemplateDocumentHtmlDefinitions(string accountId, string documentId, string templateId, map<string|string[]> headers = {}) returns DocumentHtmlDefinitionOriginals|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/documents/${getEncodedUri(documentId)}/html_definitions`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns document page images based on input.
    #
    # + accountId - (Required) The external account number (int) or account ID GUID
    # + documentId - (Required) The ID of the document
    # + templateId - (Required) The ID of the template
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listTemplatePageImages(string accountId, string documentId, string templateId, map<string|string[]> headers = {}, *ListTemplatePageImagesQueries queries) returns PageImages|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/documents/${getEncodedUri(documentId)}/pages`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes a page from a document in an template.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The unique ID of the document within the envelope.
    # Unlike other IDs in the eSignature API,
    # you specify the `documentId` yourself.
    # Typically the first document has the ID
    # `1`, the second document `2`, and so on,
    # but you can use any numbering scheme
    # that fits within a 32-bit signed integer
    # (1 through 2147483647).
    # Tab objects have a `documentId` property
    # that specifies the document on which to place
    # the tab
    # + pageNumber - The page number being accessed
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + payload - Page request used to delete a page from a document in an template 
    # + return - A successful response or an error 
    remote isolated function deleteTemplatePage(string accountId, string documentId, string pageNumber, string templateId, PageRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/documents/${getEncodedUri(documentId)}/pages/${getEncodedUri(pageNumber)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->delete(resourcePath, request, headers);
    }

    # Gets a page image from a template for display.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The unique ID of the document within the envelope.
    # Unlike other IDs in the eSignature API,
    # you specify the `documentId` yourself.
    # Typically the first document has the ID
    # `1`, the second document `2`, and so on,
    # but you can use any numbering scheme
    # that fits within a 32-bit signed integer
    # (1 through 2147483647).
    # Tab objects have a `documentId` property
    # that specifies the document on which to place
    # the tab
    # + pageNumber - The page number being accessed
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getTemplatePageImage(string accountId, string documentId, string pageNumber, string templateId, map<string|string[]> headers = {}, *GetTemplatePageImageQueries queries) returns byte[]|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/documents/${getEncodedUri(documentId)}/pages/${getEncodedUri(pageNumber)}/page_image`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Rotates page image from a template for display.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The unique ID of the document within the envelope.
    # Unlike other IDs in the eSignature API,
    # you specify the `documentId` yourself.
    # Typically the first document has the ID
    # `1`, the second document `2`, and so on,
    # but you can use any numbering scheme
    # that fits within a 32-bit signed integer
    # (1 through 2147483647).
    # Tab objects have a `documentId` property
    # that specifies the document on which to place
    # the tab
    # + pageNumber - The page number being accessed
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + payload - Page request used to rotate page image from a template for display 
    # + return - A successful response or an error 
    remote isolated function rotateTemplatePageImage(string accountId, string documentId, string pageNumber, string templateId, PageRequest payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/documents/${getEncodedUri(documentId)}/pages/${getEncodedUri(pageNumber)}/page_image`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Returns tabs on the specified page.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The unique ID of the document within the envelope.
    # Unlike other IDs in the eSignature API,
    # you specify the `documentId` yourself.
    # Typically the first document has the ID
    # `1`, the second document `2`, and so on,
    # but you can use any numbering scheme
    # that fits within a 32-bit signed integer
    # (1 through 2147483647).
    # Tab objects have a `documentId` property
    # that specifies the document on which to place
    # the tab
    # + pageNumber - The page number being accessed
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listTemplatePageTabs(string accountId, string documentId, string pageNumber, string templateId, map<string|string[]> headers = {}) returns TemplateDocumentTabs|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/documents/${getEncodedUri(documentId)}/pages/${getEncodedUri(pageNumber)}/tabs`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a preview of the responsive version of a template document.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The unique ID of the document within the envelope.
    # Unlike other IDs in the eSignature API,
    # you specify the `documentId` yourself.
    # Typically the first document has the ID
    # `1`, the second document `2`, and so on,
    # but you can use any numbering scheme
    # that fits within a 32-bit signed integer
    # (1 through 2147483647).
    # Tab objects have a `documentId` property
    # that specifies the document on which to place
    # the tab
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + payload - Document HTML definition used to create a preview of the responsive version of a template document 
    # + return - A successful response or an error 
    remote isolated function createTemplateDocResponsivePreview(string accountId, string documentId, string templateId, DocumentHtmlDefinition payload, map<string|string[]> headers = {}) returns DocumentHtmlDefinitions|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/documents/${getEncodedUri(documentId)}/responsive_html_preview`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns tabs on a template.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The unique ID of the document within the envelope.
    # Unlike other IDs in the eSignature API,
    # you specify the `documentId` yourself.
    # Typically the first document has the ID
    # `1`, the second document `2`, and so on,
    # but you can use any numbering scheme
    # that fits within a 32-bit signed integer
    # (1 through 2147483647).
    # Tab objects have a `documentId` property
    # that specifies the document on which to place
    # the tab
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listTemplateDocumentTabs(string accountId, string documentId, string templateId, map<string|string[]> headers = {}, *ListTemplateDocumentTabsQueries queries) returns TemplateDocumentTabs|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/documents/${getEncodedUri(documentId)}/tabs`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the tabs for a template.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The unique ID of the document within the envelope.
    # Unlike other IDs in the eSignature API,
    # you specify the `documentId` yourself.
    # Typically the first document has the ID
    # `1`, the second document `2`, and so on,
    # but you can use any numbering scheme
    # that fits within a 32-bit signed integer
    # (1 through 2147483647).
    # Tab objects have a `documentId` property
    # that specifies the document on which to place
    # the tab
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + payload - Template tabs used to update the tabs for a template 
    # + return - A successful response or an error 
    remote isolated function updateTemplateDocumentTabs(string accountId, string documentId, string templateId, TemplateTabs payload, map<string|string[]> headers = {}) returns Tabs|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/documents/${getEncodedUri(documentId)}/tabs`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Adds tabs to a document in a template.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The unique ID of the document within the envelope.
    # Unlike other IDs in the eSignature API,
    # you specify the `documentId` yourself.
    # Typically the first document has the ID
    # `1`, the second document `2`, and so on,
    # but you can use any numbering scheme
    # that fits within a 32-bit signed integer
    # (1 through 2147483647).
    # Tab objects have a `documentId` property
    # that specifies the document on which to place
    # the tab
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + payload - Template tabs used to add tabs to a document in a template 
    # + return - A successful response or an error 
    remote isolated function createTemplateDocumentTabs(string accountId, string documentId, string templateId, TemplateTabs payload, map<string|string[]> headers = {}) returns Tabs|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/documents/${getEncodedUri(documentId)}/tabs`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes tabs from a template.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + documentId - The unique ID of the document within the envelope.
    # Unlike other IDs in the eSignature API,
    # you specify the `documentId` yourself.
    # Typically the first document has the ID
    # `1`, the second document `2`, and so on,
    # but you can use any numbering scheme
    # that fits within a 32-bit signed integer
    # (1 through 2147483647).
    # Tab objects have a `documentId` property
    # that specifies the document on which to place
    # the tab
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + payload - Template tabs used to delete tabs from a template 
    # + return - A successful response or an error 
    remote isolated function deleteTemplateDocumentTabs(string accountId, string documentId, string templateId, TemplateTabs payload, map<string|string[]> headers = {}) returns Tabs|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/documents/${getEncodedUri(documentId)}/tabs`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->delete(resourcePath, request, headers);
    }

    # Gets the Original HTML Definition used to generate the Responsive HTML for the template.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listTemplateHtmlDefinitions(string accountId, string templateId, map<string|string[]> headers = {}) returns DocumentHtmlDefinitionOriginals|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/html_definitions`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Gets template lock information.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getTemplateLock(string accountId, string templateId, map<string|string[]> headers = {}) returns LockInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/lock`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates a template lock.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + payload - Lock request used to update a template lock 
    # + return - A successful response or an error 
    remote isolated function updateTemplateLock(string accountId, string templateId, LockRequest payload, map<string|string[]> headers = {}) returns LockInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/lock`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Locks a template.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + payload - Lock request used to lock a template 
    # + return - A successful response or an error 
    remote isolated function createTemplateLock(string accountId, string templateId, LockRequest payload, map<string|string[]> headers = {}) returns LockInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/lock`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes a template lock.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + payload - Lock request used to delete a template lock 
    # + return - A successful response or an error 
    remote isolated function deleteTemplateLock(string accountId, string templateId, LockRequest payload, map<string|string[]> headers = {}) returns LockInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/lock`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->delete(resourcePath, request, headers);
    }

    # Gets template notification information.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getTemplateNotification(string accountId, string templateId, map<string|string[]> headers = {}) returns Notification|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/notification`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the notification  structure for an existing template.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + payload - Template notification request used to update the notification structure for an existing template 
    # + return - A successful response or an error 
    remote isolated function updateTemplateNotification(string accountId, string templateId, TemplateNotificationRequest payload, map<string|string[]> headers = {}) returns Notification|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/notification`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Gets recipient information from a template.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listTemplateRecipients(string accountId, string templateId, map<string|string[]> headers = {}, *ListTemplateRecipientsQueries queries) returns Recipients|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/recipients`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates recipients in a template.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - Template recipients used to update recipients in a template 
    # + return - A successful response or an error 
    remote isolated function updateTemplateRecipients(string accountId, string templateId, TemplateRecipients payload, map<string|string[]> headers = {}, *UpdateTemplateRecipientsQueries queries) returns RecipientsUpdateSummary|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/recipients`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Adds tabs for a recipient.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - Template recipients used to add tabs for a recipient 
    # + return - A successful response or an error 
    remote isolated function createTemplateRecipients(string accountId, string templateId, TemplateRecipients payload, map<string|string[]> headers = {}, *CreateTemplateRecipientsQueries queries) returns Recipients|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/recipients`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes recipients from a template.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + payload - Template recipients used to delete recipients from a template 
    # + return - A successful response or an error 
    remote isolated function deleteTemplateRecipients(string accountId, string templateId, TemplateRecipients payload, map<string|string[]> headers = {}) returns Recipients|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/recipients`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->delete(resourcePath, request, headers);
    }

    # Deletes the specified recipient file from a template.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + recipientId - A local reference used to map
    # recipients to other objects, such as specific
    # document tabs.
    # A `recipientId` must be
    # either an integer or a GUID,
    # and the `recipientId` must be
    # unique within an envelope.
    # For example, many envelopes assign the first recipient
    # a `recipientId` of `1`
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + payload - Template recipients used to delete the specified recipient file from a template 
    # + return - A successful response or an error 
    remote isolated function deleteTemplateRecipient(string accountId, string recipientId, string templateId, TemplateRecipients payload, map<string|string[]> headers = {}) returns Recipients|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/recipients/${getEncodedUri(recipientId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->delete(resourcePath, request, headers);
    }

    # Returns document visibility for a template recipient
    #
    # + accountId - The external account number (int) or account ID GUID
    # + recipientId - A local reference used to map
    # recipients to other objects, such as specific
    # document tabs.
    # A `recipientId` must be
    # either an integer or a GUID,
    # and the `recipientId` must be
    # unique within an envelope.
    # For example, many envelopes assign the first recipient
    # a `recipientId` of `1`
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getTemplateRecipientVisibility(string accountId, string recipientId, string templateId, map<string|string[]> headers = {}) returns DocumentVisibilityList|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/recipients/${getEncodedUri(recipientId)}/document_visibility`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates document visibility for a template recipient
    #
    # + accountId - The external account number (int) or account ID GUID
    # + recipientId - A local reference used to map
    # recipients to other objects, such as specific
    # document tabs.
    # A `recipientId` must be
    # either an integer or a GUID,
    # and the `recipientId` must be
    # unique within an envelope.
    # For example, many envelopes assign the first recipient
    # a `recipientId` of `1`
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + payload - Template document visibility list used to update document visibility for a template recipient 
    # + return - A successful response or an error 
    remote isolated function updateTemplateRecipientVisibility(string accountId, string recipientId, string templateId, TemplateDocumentVisibilityList payload, map<string|string[]> headers = {}) returns TemplateDocumentVisibilityList|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/recipients/${getEncodedUri(recipientId)}/document_visibility`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Gets the tabs information for a signer or sign-in-person recipient in a template.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + recipientId - A local reference used to map
    # recipients to other objects, such as specific
    # document tabs.
    # A `recipientId` must be
    # either an integer or a GUID,
    # and the `recipientId` must be
    # unique within an envelope.
    # For example, many envelopes assign the first recipient
    # a `recipientId` of `1`
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listTemplateRecipientTabs(string accountId, string recipientId, string templateId, map<string|string[]> headers = {}, *ListTemplateRecipientTabsQueries queries) returns Tabs|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/recipients/${getEncodedUri(recipientId)}/tabs`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the tabs for a recipient.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + recipientId - A local reference used to map
    # recipients to other objects, such as specific
    # document tabs.
    # A `recipientId` must be
    # either an integer or a GUID,
    # and the `recipientId` must be
    # unique within an envelope.
    # For example, many envelopes assign the first recipient
    # a `recipientId` of `1`
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + payload - Template tabs used to update the tabs for a recipient 
    # + return - A successful response or an error 
    remote isolated function updateTemplateRecipientTabs(string accountId, string recipientId, string templateId, TemplateTabs payload, map<string|string[]> headers = {}) returns Tabs|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/recipients/${getEncodedUri(recipientId)}/tabs`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Adds tabs for a recipient.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + recipientId - A local reference used to map
    # recipients to other objects, such as specific
    # document tabs.
    # A `recipientId` must be
    # either an integer or a GUID,
    # and the `recipientId` must be
    # unique within an envelope.
    # For example, many envelopes assign the first recipient
    # a `recipientId` of `1`
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + payload - Template tabs used to add tabs for a recipient 
    # + return - A successful response or an error 
    remote isolated function createTemplateRecipientTabs(string accountId, string recipientId, string templateId, TemplateTabs payload, map<string|string[]> headers = {}) returns Tabs|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/recipients/${getEncodedUri(recipientId)}/tabs`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes the tabs associated with a recipient in a template.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + recipientId - A local reference used to map
    # recipients to other objects, such as specific
    # document tabs.
    # A `recipientId` must be
    # either an integer or a GUID,
    # and the `recipientId` must be
    # unique within an envelope.
    # For example, many envelopes assign the first recipient
    # a `recipientId` of `1`
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + payload - Template tabs used to delete the tabs associated with a recipient in a template 
    # + return - A successful response or an error 
    remote isolated function deleteTemplateRecipientTabs(string accountId, string recipientId, string templateId, TemplateTabs payload, map<string|string[]> headers = {}) returns Tabs|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/recipients/${getEncodedUri(recipientId)}/tabs`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->delete(resourcePath, request, headers);
    }

    # Updates document visibility for template recipients
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + payload - Template document visibility list used to update document visibility for template recipients 
    # + return - A successful response or an error 
    remote isolated function updateTemplateRecipientsVisibility(string accountId, string templateId, TemplateDocumentVisibilityList payload, map<string|string[]> headers = {}) returns TemplateDocumentVisibilityList|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/recipients/document_visibility`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Creates a preview of the responsive versions of all of the documents associated with a template.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + payload - Document HTML definition for this request 
    # + return - A successful response or an error 
    remote isolated function createTemplateResponsivePreview(string accountId, string templateId, DocumentHtmlDefinition payload, map<string|string[]> headers = {}) returns DocumentHtmlDefinitions|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/responsive_html_preview`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Gets a URL for a template edit view.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + payload - Template view request used to get a URL for a template edit view 
    # + return - A successful response or an error 
    remote isolated function createTemplateEditView(string accountId, string templateId, TemplateViewRequest payload, map<string|string[]> headers = {}) returns ViewUrl|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/views/edit`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Creates a template recipient preview.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + payload - Recipient preview request used to create a template recipient preview 
    # + return - A successful response or an error 
    remote isolated function createTemplateRecipientPreview(string accountId, string templateId, RecipientPreviewRequest payload, map<string|string[]> headers = {}) returns ViewUrl|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/views/recipient_preview`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns the workflow definition for a template.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getTemplateWorkflow(string accountId, string templateId, map<string|string[]> headers = {}) returns Workflow|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/workflow`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the workflow definition for a template.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + payload - Workflow used to update the workflow definition for a template 
    # + return - A successful response or an error 
    remote isolated function updateTemplateWorkflow(string accountId, string templateId, Workflow payload, map<string|string[]> headers = {}) returns Workflow|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/workflow`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete the workflow definition for a template.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteTemplateWorkflow(string accountId, string templateId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/workflow`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Returns the scheduled sending rules for a template's workflow definition.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getTemplateScheduledSending(string accountId, string templateId, map<string|string[]> headers = {}) returns ScheduledSending|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/workflow/scheduledSending`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the scheduled sending rules for a template's workflow definition.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + payload - An object that describes the settings for scheduled sending 
    # + return - A successful response or an error 
    remote isolated function updateTemplateScheduledSending(string accountId, string templateId, ScheduledSending payload, map<string|string[]> headers = {}) returns ScheduledSending|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/workflow/scheduledSending`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes the scheduled sending rules for the template's workflow.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteTemplateScheduledSending(string accountId, string templateId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/workflow/scheduledSending`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Adds a new step to a template's workflow.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + headers - Headers to be sent with the request 
    # + payload - Workflow step used to add a new step to a template's workflow 
    # + return - A successful response or an error 
    remote isolated function createTemplateWorkflowStep(string accountId, string templateId, WorkflowStep payload, map<string|string[]> headers = {}) returns WorkflowStep|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/workflow/steps`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns a specified workflow step for a specified envelope.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + workflowStepId - The ID of the workflow step
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getTemplateWorkflowStep(string accountId, string templateId, string workflowStepId, map<string|string[]> headers = {}) returns WorkflowStep|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/workflow/steps/${getEncodedUri(workflowStepId)}`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates a specified workflow step for a template.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + workflowStepId - The ID of the workflow step
    # + headers - Headers to be sent with the request 
    # + payload - Workflow step used to update a specified workflow step for a template 
    # + return - A successful response or an error 
    remote isolated function updateTemplateWorkflowStep(string accountId, string templateId, string workflowStepId, WorkflowStep payload, map<string|string[]> headers = {}) returns WorkflowStep|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/workflow/steps/${getEncodedUri(workflowStepId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes a workflow step from an template's workflow definition.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + workflowStepId - The ID of the workflow step
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteTemplateWorkflowStep(string accountId, string templateId, string workflowStepId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/workflow/steps/${getEncodedUri(workflowStepId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Returns the delayed routing rules for a template's workflow step definition.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + workflowStepId - The ID of the workflow step
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getTemplateDelayedRouting(string accountId, string templateId, string workflowStepId, map<string|string[]> headers = {}) returns DelayedRouting|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/workflow/steps/${getEncodedUri(workflowStepId)}/delayedRouting`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the delayed routing rules for a template's workflow step.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + workflowStepId - The ID of the workflow step
    # + headers - Headers to be sent with the request 
    # + payload - A complex element that specifies the delayed routing settings for the workflow step 
    # + return - A successful response or an error 
    remote isolated function updateTemplateDelayedRouting(string accountId, string templateId, string workflowStepId, DelayedRouting payload, map<string|string[]> headers = {}) returns DelayedRouting|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/workflow/steps/${getEncodedUri(workflowStepId)}/delayedRouting`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes the delayed routing rules for the specified template workflow step.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + templateId - The ID of the template
    # + workflowStepId - The ID of the workflow step
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteTemplateDelayedRouting(string accountId, string templateId, string workflowStepId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/${getEncodedUri(templateId)}/workflow/steps/${getEncodedUri(workflowStepId)}/delayedRouting`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Updates the auto-match setting for multiple templates by using the auto-match endpoint.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Template auto match list for this request 
    # + return - A successful response or an error 
    remote isolated function applyTemplatesAutoMatch(string accountId, TemplateAutoMatchList payload, map<string|string[]> headers = {}) returns TemplateAutoMatchList|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/templates/auto_match`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Gets a list of unsupported file types.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listUnsupportedFileTypes(string accountId, map<string|string[]> headers = {}) returns FileTypeList|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/unsupported_file_types`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the list of users for the specified account. You can filter the users list to get specific users.
    #
    # + accountId - (Required) The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listUsers(string accountId, map<string|string[]> headers = {}, *ListUsersQueries queries) returns UserInformationList|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Changes one or more users in the specified account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - User information list used to change one or more users in the specified account 
    # + return - A successful response or an error 
    remote isolated function updateUsers(string accountId, UserInformationList payload, map<string|string[]> headers = {}, *UpdateUsersQueries queries) returns UserInformationList|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Adds new users to the specified account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - New users definition used to add new users to the specified account 
    # + return - A successful response or an error 
    remote isolated function createUsers(string accountId, NewUsersDefinition payload, map<string|string[]> headers = {}) returns NewUsersSummary|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Closes one or more users in the account.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - User info list used to close one or more users in the account 
    # + return - A successful response or an error 
    remote isolated function closeUsers(string accountId, UserInfoList payload, map<string|string[]> headers = {}, *CloseUsersQueries queries) returns UsersResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->delete(resourcePath, request, headers);
    }

    # Gets the user information for a specified user using a userId (GUID). To find a user based on their email address, use the list endpoint.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + userId - The ID of the user to access.
    # **Note:** Users can only access their own information. A user, even one with Admin rights, cannot access another user's settings
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getUser(string accountId, string userId, map<string|string[]> headers = {}, *GetUserQueries queries) returns UserInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates user information for the specified user.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + userId - The ID of the user to access.
    # **Note:** Users can only access their own information. A user, even one with Admin rights, cannot access another user's settings
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - User information used to update user information for the specified user 
    # + return - A successful response or an error 
    remote isolated function updateUser(string accountId, string userId, UserInformation payload, map<string|string[]> headers = {}, *UpdateUserQueries queries) returns UserInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Creates a user authorization.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + userId - The ID of the principal user
    # + headers - Headers to be sent with the request 
    # + payload - User authorization create request used to create a user authorization 
    # + return - A successful response or an error 
    remote isolated function createUserAuthorization(string accountId, string userId, UserAuthorizationCreateRequest payload, map<string|string[]> headers = {}) returns UserAuthorization|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}/authorization`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Returns the user authorization for a given authorization ID.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + authorizationId - The ID of the user authorization
    # + userId - The ID of the principal user
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getUserAuthorization(string accountId, string authorizationId, string userId, map<string|string[]> headers = {}) returns UserAuthorization|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}/authorization/${getEncodedUri(authorizationId)}`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the start or end date for a user authorization.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + authorizationId - The ID of the user authorization
    # + userId - The ID of the principal user
    # + headers - Headers to be sent with the request 
    # + payload - User authorization update request used to update the start or end date for a user authorization 
    # + return - A successful response or an error 
    remote isolated function updateUserAuthorization(string accountId, string authorizationId, string userId, UserAuthorizationUpdateRequest payload, map<string|string[]> headers = {}) returns UserAuthorization|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}/authorization/${getEncodedUri(authorizationId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes the user authorization.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + authorizationId - The ID of the user authorization
    # + userId - The ID of the principal user
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteUserAuthorization(string accountId, string authorizationId, string userId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}/authorization/${getEncodedUri(authorizationId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Returns the authorizations for which the specified user is the principal user.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + userId - The ID of the principal user
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listPrincipalUserAuthorizations(string accountId, string userId, map<string|string[]> headers = {}, *ListPrincipalUserAuthorizationsQueries queries) returns UserAuthorizations|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}/authorizations`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Create or update multiple user authorizations.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + userId - The ID of the principal user
    # + headers - Headers to be sent with the request 
    # + payload - User authorizations request used to create or update multiple user authorizations 
    # + return - A successful response or an error 
    remote isolated function upsertUserAuthorizations(string accountId, string userId, UserAuthorizationsRequest payload, map<string|string[]> headers = {}) returns UserAuthorizationsResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}/authorizations`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Delete multiple user authorizations.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + userId - The ID of the principal user
    # + headers - Headers to be sent with the request 
    # + payload - User authorizations delete request used to delete multiple user authorizations 
    # + return - A successful response or an error 
    remote isolated function deleteUserAuthorizations(string accountId, string userId, UserAuthorizationsDeleteRequest payload, map<string|string[]> headers = {}) returns UserAuthorizationsDeleteResponse|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}/authorizations`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->delete(resourcePath, request, headers);
    }

    # Returns the authorizations for which the specified user is the agent user.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + userId - The user who is acting as the agent
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listAgentUserAuthorizations(string accountId, string userId, map<string|string[]> headers = {}, *ListAgentUserAuthorizationsQueries queries) returns UserAuthorizations|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}/authorizations/agent`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Get the Cloud Storage Provider configuration for the specified user.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + userId - The ID of the user to access.
    # **Note:** Users can only access their own information. A user, even one with Admin rights, cannot access another user's settings
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listCloudStorageProviders(string accountId, string userId, map<string|string[]> headers = {}, *ListCloudStorageProvidersQueries queries) returns CloudStorageProviders|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}/cloud_storage`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Configures the redirect URL information  for one or more cloud storage providers for the specified user.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + userId - The ID of the user to access.
    # **Note:** Users can only access their own information. A user, even one with Admin rights, cannot access another user's settings
    # + headers - Headers to be sent with the request 
    # + payload - Cloud storage providers for this request 
    # + return - A successful response or an error 
    remote isolated function createCloudStorageProvider(string accountId, string userId, CloudStorageProviders payload, map<string|string[]> headers = {}) returns CloudStorageProviders|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}/cloud_storage`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Deletes the user authentication information for one or more cloud storage providers.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + userId - The ID of the user to access.
    # **Note:** Users can only access their own information. A user, even one with Admin rights, cannot access another user's settings
    # + headers - Headers to be sent with the request 
    # + payload - Cloud storage providers for this request 
    # + return - A successful response or an error 
    remote isolated function deleteCloudStorageProviders(string accountId, string userId, CloudStorageProviders payload, map<string|string[]> headers = {}) returns CloudStorageProviders|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}/cloud_storage`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->delete(resourcePath, request, headers);
    }

    # Gets the specified Cloud Storage Provider configuration for the User.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + serviceId - The ID of the service to access. 
    # Valid values are the service name ("Box") or the numerical serviceId ("4136")
    # + userId - The ID of the user to access.
    # **Note:** Users can only access their own information. A user, even one with Admin rights, cannot access another user's settings
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getCloudStorageProvider(string accountId, string serviceId, string userId, map<string|string[]> headers = {}, *GetCloudStorageProviderQueries queries) returns CloudStorageProviders|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}/cloud_storage/${getEncodedUri(serviceId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes the user authentication information for the specified cloud storage provider.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + serviceId - The ID of the service to access. 
    # Valid values are the service name ("Box") or the numerical serviceId ("4136")
    # + userId - The ID of the user to access.
    # **Note:** Users can only access their own information. A user, even one with Admin rights, cannot access another user's settings
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteCloudStorageProvider(string accountId, string serviceId, string userId, map<string|string[]> headers = {}) returns CloudStorageProviders|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}/cloud_storage/${getEncodedUri(serviceId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Retrieves a list of all the items in a specified folder from the specified cloud storage provider.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + serviceId - The ID of the service to access. 
    # Valid values are the service name ("Box") or the numerical serviceId ("4136")
    # + userId - The ID of the user to access.
    # **Note:** Users can only access their own information. A user, even one with Admin rights, cannot access another user's settings
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listCloudStorageItems(string accountId, string serviceId, string userId, map<string|string[]> headers = {}, *ListCloudStorageItemsQueries queries) returns ExternalFolder|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}/cloud_storage/${getEncodedUri(serviceId)}/folders`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Gets a list of items from a cloud storage provider.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + folderId - The ID of the folder
    # + serviceId - The ID of the service to access. 
    # Valid values are the service name ("Box") or the numerical serviceId ("4136")
    # + userId - The ID of the user to access.
    # **Note:** Users can only access their own information. A user, even one with Admin rights, cannot access another user's settings
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getCloudStorageFolder(string accountId, string folderId, string serviceId, string userId, map<string|string[]> headers = {}, *GetCloudStorageFolderQueries queries) returns ExternalFolder|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}/cloud_storage/${getEncodedUri(serviceId)}/folders/${getEncodedUri(folderId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the custom user settings for a specified user.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + userId - The ID of the user to access.
    # **Note:** Users can only access their own information. A user, even one with Admin rights, cannot access another user's settings
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getCustomSettings(string accountId, string userId, map<string|string[]> headers = {}) returns CustomSettingsInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}/custom_settings`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Adds or updates custom user settings for the specified user.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + userId - The ID of the user to access.
    # **Note:** Users can only access their own information. A user, even one with Admin rights, cannot access another user's settings
    # + headers - Headers to be sent with the request 
    # + payload - Custom settings information used to add or updates custom user settings for the specified user 
    # + return - A successful response or an error 
    remote isolated function updateCustomSettings(string accountId, string userId, CustomSettingsInformation payload, map<string|string[]> headers = {}) returns CustomSettingsInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}/custom_settings`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes custom user settings for a specified user.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + userId - The ID of the user to access.
    # **Note:** Users can only access their own information. A user, even one with Admin rights, cannot access another user's settings
    # + headers - Headers to be sent with the request 
    # + payload - Custom settings information used to delete custom user settings for a specified user 
    # + return - A successful response or an error 
    remote isolated function deleteCustomSettings(string accountId, string userId, CustomSettingsInformation payload, map<string|string[]> headers = {}) returns CustomSettingsInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}/custom_settings`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->delete(resourcePath, request, headers);
    }

    # Retrieves the user profile for a specified user.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + userId - The ID of the user to access.
    # **Note:** Users can only access their own information. A user, even one with Admin rights, cannot access another user's settings
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getUserProfile(string accountId, string userId, map<string|string[]> headers = {}) returns UserProfile|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}/profile`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the user profile information for the specified user.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + userId - The ID of the user to access.
    # **Note:** Users can only access their own information. A user, even one with Admin rights, cannot access another user's settings
    # + headers - Headers to be sent with the request 
    # + payload - User profile used to update the user profile information for the specified user 
    # + return - A successful response or an error 
    remote isolated function updateUserProfile(string accountId, string userId, UserProfile payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}/profile`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Retrieves the user profile image for the specified user.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + userId - The ID of the user to access.
    # **Note:** Users can only access their own information. A user, even one with Admin rights, cannot access another user's settings
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getUserProfileImage(string accountId, string userId, map<string|string[]> headers = {}, *GetUserProfileImageQueries queries) returns http:Response|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}/profile/image`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the user profile image for a specified user.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + userId - The ID of the user to access.
    # **Note:** Users can only access their own information. A user, even one with Admin rights, cannot access another user's settings
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function updateUserProfileImage(string accountId, string userId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}/profile/image`;
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes the user profile image for the specified user.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + userId - The ID of the user to access.
    # **Note:** Users can only access their own information. A user, even one with Admin rights, cannot access another user's settings
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteUserProfileImage(string accountId, string userId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}/profile/image`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Gets the user account settings for a specified user.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + userId - The ID of the user to access.
    # **Note:** Users can only access their own information. A user, even one with Admin rights, cannot access another user's settings
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getUserSettings(string accountId, string userId, map<string|string[]> headers = {}) returns UserSettingsInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}/settings`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the user account settings for a specified user.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + userId - The ID of the user to access.
    # **Note:** Users can only access their own information. A user, even one with Admin rights, cannot access another user's settings
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - User settings information used to update the user account settings for a specified user 
    # + return - A successful response or an error 
    remote isolated function updateUserSettings(string accountId, string userId, UserSettingsInformation payload, map<string|string[]> headers = {}, *UpdateUserSettingsQueries queries) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}/settings`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Retrieves a list of signature definitions for a user.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + userId - The ID of the user to access.
    # **Note:** Users can only access their own information. A user, even one with Admin rights, cannot access another user's settings
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listUserSignatures(string accountId, string userId, map<string|string[]> headers = {}, *ListUserSignaturesQueries queries) returns UserSignaturesInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}/signatures`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Adds/updates a user signature.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + userId - The ID of the user to access.
    # **Note:** Users can only access their own information. A user, even one with Admin rights, cannot access another user's settings
    # + headers - Headers to be sent with the request 
    # + payload - User signatures information used to adds/update a user signature 
    # + return - A successful response or an error 
    remote isolated function updateUserSignatures(string accountId, string userId, UserSignaturesInformation payload, map<string|string[]> headers = {}) returns UserSignaturesInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}/signatures`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Adds user Signature and initials images to a Signature.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + userId - The ID of the user to access.
    # **Note:** Users can only access their own information. A user, even one with Admin rights, cannot access another user's settings
    # + headers - Headers to be sent with the request 
    # + payload - User signatures information used to add user Signature and initials images to a Signature 
    # + return - A successful response or an error 
    remote isolated function createUserSignatures(string accountId, string userId, UserSignaturesInformation payload, map<string|string[]> headers = {}) returns UserSignaturesInformation|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}/signatures`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Gets the user signature information for the specified user.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + signatureId - The ID of the account stamp
    # + userId - The ID of the user to access.
    # **Note:** Users can only access their own information. A user, even one with Admin rights, cannot access another user's settings
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getUserSignature(string accountId, string signatureId, string userId, map<string|string[]> headers = {}) returns UserSignature|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}/signatures/${getEncodedUri(signatureId)}`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the user signature for a specified user.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + signatureId - The ID of the account stamp
    # + userId - The ID of the user to access.
    # **Note:** Users can only access their own information. A user, even one with Admin rights, cannot access another user's settings
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + payload - User signature definition used to update the user signature for a specified user 
    # + return - A successful response or an error 
    remote isolated function updateUserSignature(string accountId, string signatureId, string userId, UserSignatureDefinition payload, map<string|string[]> headers = {}, *UpdateUserSignatureQueries queries) returns UserSignature|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}/signatures/${getEncodedUri(signatureId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Removes removes signature information for the specified user.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + signatureId - The ID of the account stamp
    # + userId - The ID of the user to access.
    # **Note:** Users can only access their own information. A user, even one with Admin rights, cannot access another user's settings
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteUserSignature(string accountId, string signatureId, string userId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}/signatures/${getEncodedUri(signatureId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Retrieves the user initials image or the  user signature image for the specified user.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + imageType - Specificies the type of image. Valid values:
    # - `stamp_image`
    # - `signature_image`
    # - `initials_image`
    # + signatureId - The ID of the account stamp
    # + userId - The ID of the user to access.
    # **Note:** Users can only access their own information. A user, even one with Admin rights, cannot access another user's settings
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getUserSignatureImage(string accountId, string imageType, string signatureId, string userId, map<string|string[]> headers = {}, *GetUserSignatureImageQueries queries) returns http:Response|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}/signatures/${getEncodedUri(signatureId)}/${getEncodedUri(imageType)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the user signature image or user initials image for the specified user.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + imageType - Specificies the type of image. Valid values:
    # - `stamp_image`
    # - `signature_image`
    # - `initials_image`
    # + signatureId - The ID of the account stamp
    # + userId - The ID of the user to access.
    # **Note:** Users can only access their own information. A user, even one with Admin rights, cannot access another user's settings
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + request - Updated image content 
    # + return - A successful response or an error 
    remote isolated function updateUserSignatureImage(string accountId, string imageType, string signatureId, string userId, http:Request request, map<string|string[]> headers = {}, *UpdateUserSignatureImageQueries queries) returns UserSignature|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}/signatures/${getEncodedUri(signatureId)}/${getEncodedUri(imageType)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        // TODO: Update the request as needed;
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes the user initials image or the  user signature image for the specified user.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + imageType - Specificies the type of image. Valid values:
    # - `stamp_image`
    # - `signature_image`
    # - `initials_image`
    # + signatureId - The ID of the account stamp
    # + userId - The ID of the user to access.
    # **Note:** Users can only access their own information. A user, even one with Admin rights, cannot access another user's settings
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteUserSignatureImage(string accountId, string imageType, string signatureId, string userId, map<string|string[]> headers = {}) returns UserSignature|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/users/${getEncodedUri(userId)}/signatures/${getEncodedUri(signatureId)}/${getEncodedUri(imageType)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Returns a URL to the Docusign eSignature web application.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Console view request used to return a URL to the Docusign eSignature web application 
    # + return - A successful response or an error 
    remote isolated function createConsoleView(string accountId, ConsoleViewRequest payload, map<string|string[]> headers = {}) returns EnvelopeViews|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/views/console`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get watermark information.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getWatermark(string accountId, map<string|string[]> headers = {}) returns Watermark|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/watermark`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Update watermark information.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - When **true,** the account has the watermark feature enabled, and the envelope is not complete, then the watermark for the account is added to the PDF documents. This option can remove the watermark.  
    # + return - A successful response or an error 
    remote isolated function updateWatermark(string accountId, Watermark payload, map<string|string[]> headers = {}) returns Watermark|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/watermark`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Get watermark preview.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - When **true,** the account has the watermark feature enabled, and the envelope is not complete, then the watermark for the account is added to the PDF documents. This option can remove the watermark.  
    # + return - A successful response or an error 
    remote isolated function previewWatermark(string accountId, Watermark payload, map<string|string[]> headers = {}) returns Watermark|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/watermark/preview`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # List Workspaces
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listWorkspaces(string accountId, map<string|string[]> headers = {}) returns WorkspaceList|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/workspaces`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Create a Workspace
    #
    # + accountId - The external account number (int) or account ID GUID
    # + headers - Headers to be sent with the request 
    # + payload - Workspace used to create a Workspace 
    # + return - A successful response or an error 
    remote isolated function createWorkspace(string accountId, Workspace payload, map<string|string[]> headers = {}) returns Workspace|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/workspaces`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Get Workspace
    #
    # + accountId - The external account number (int) or account ID GUID
    # + workspaceId - The ID of the workspace
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getWorkspace(string accountId, string workspaceId, map<string|string[]> headers = {}) returns Workspace|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/workspaces/${getEncodedUri(workspaceId)}`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Update Workspace
    #
    # + accountId - The external account number (int) or account ID GUID
    # + workspaceId - The ID of the workspace
    # + headers - Headers to be sent with the request 
    # + payload - Workspace used to update workspace 
    # + return - A successful response or an error 
    remote isolated function updateWorkspace(string accountId, string workspaceId, Workspace payload, map<string|string[]> headers = {}) returns Workspace|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/workspaces/${getEncodedUri(workspaceId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Delete Workspace
    #
    # + accountId - The external account number (int) or account ID GUID
    # + workspaceId - The ID of the workspace
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteWorkspace(string accountId, string workspaceId, map<string|string[]> headers = {}) returns Workspace|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/workspaces/${getEncodedUri(workspaceId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # List workspace folder contents
    #
    # + accountId - The external account number (int) or account ID GUID
    # + folderId - The ID of the folder
    # + workspaceId - The ID of the workspace
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getWorkspaceFolder(string accountId, string folderId, string workspaceId, map<string|string[]> headers = {}, *GetWorkspaceFolderQueries queries) returns WorkspaceFolderContents|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/workspaces/${getEncodedUri(workspaceId)}/folders/${getEncodedUri(folderId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes files or sub-folders from a workspace.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + folderId - The ID of the folder
    # + workspaceId - The ID of the workspace
    # + headers - Headers to be sent with the request 
    # + payload - Workspace item list used to delete files or sub-folders from a workspace 
    # + return - A successful response or an error 
    remote isolated function deleteWorkspaceItems(string accountId, string folderId, string workspaceId, WorkspaceItemList payload, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/workspaces/${getEncodedUri(workspaceId)}/folders/${getEncodedUri(folderId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->delete(resourcePath, request, headers);
    }

    # Creates a workspace file.
    #
    # + accountId - The external account number (int) or account ID GUID
    # + folderId - The ID of the folder
    # + workspaceId - The ID of the workspace
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function createWorkspaceFile(string accountId, string folderId, string workspaceId, map<string|string[]> headers = {}) returns WorkspaceItem|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/workspaces/${getEncodedUri(workspaceId)}/folders/${getEncodedUri(folderId)}/files`;
        http:Request request = new;
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Gets a workspace file
    #
    # + accountId - The external account number (int) or account ID GUID
    # + fileId - The ID of the file
    # + folderId - The ID of the folder
    # + workspaceId - The ID of the workspace
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getWorkspaceFile(string accountId, string fileId, string folderId, string workspaceId, map<string|string[]> headers = {}, *GetWorkspaceFileQueries queries) returns error? {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/workspaces/${getEncodedUri(workspaceId)}/folders/${getEncodedUri(folderId)}/files/${getEncodedUri(fileId)}`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Update workspace file or folder metadata
    #
    # + accountId - The external account number (int) or account ID GUID
    # + fileId - The ID of the file
    # + folderId - The ID of the folder
    # + workspaceId - The ID of the workspace
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function updateWorkspaceFile(string accountId, string fileId, string folderId, string workspaceId, map<string|string[]> headers = {}) returns WorkspaceItem|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/workspaces/${getEncodedUri(workspaceId)}/folders/${getEncodedUri(folderId)}/files/${getEncodedUri(fileId)}`;
        http:Request request = new;
        return self.clientEp->put(resourcePath, request, headers);
    }

    # List File Pages
    #
    # + accountId - The external account number (int) or account ID GUID
    # + fileId - The ID of the file
    # + folderId - The ID of the folder
    # + workspaceId - The ID of the workspace
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listWorkspaceFilePages(string accountId, string fileId, string folderId, string workspaceId, map<string|string[]> headers = {}, *ListWorkspaceFilePagesQueries queries) returns PageImages|error {
        string resourcePath = string `/v2.1/accounts/${getEncodedUri(accountId)}/workspaces/${getEncodedUri(workspaceId)}/folders/${getEncodedUri(folderId)}/files/${getEncodedUri(fileId)}/pages`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Retrieves the account provisioning information for the account.
    #
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getAccountProvisioning(map<string|string[]> headers = {}) returns ProvisioningInformation|error {
        string resourcePath = string `/v2.1/accounts/provisioning`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Gets a list of available billing plans.
    #
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listBillingPlans(map<string|string[]> headers = {}) returns BillingPlansResponse|error {
        string resourcePath = string `/v2.1/billing_plans`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Gets billing plan details.
    #
    # + billingPlanId - The ID of the billing plan being accessed
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getBillingPlan(string billingPlanId, map<string|string[]> headers = {}) returns BillingPlanResponse|error {
        string resourcePath = string `/v2.1/billing_plans/${getEncodedUri(billingPlanId)}`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Gets settings for a  notary user.
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getNotary(map<string|string[]> headers = {}, *GetNotaryQueries queries) returns NotaryResult|error {
        string resourcePath = string `/v2.1/current_user/notary`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates notary information for the current user.
    #
    # + headers - Headers to be sent with the request 
    # + payload - Notary used to update notary information for the current user 
    # + return - A successful response or an error 
    remote isolated function updateNotary(Notary payload, map<string|string[]> headers = {}) returns Notary|error {
        string resourcePath = string `/v2.1/current_user/notary`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Registers the current user as a notary.
    #
    # + headers - Headers to be sent with the request 
    # + payload - Notary used to register the current user as a notary 
    # + return - A successful response or an error 
    remote isolated function registerNotary(Notary payload, map<string|string[]> headers = {}) returns Notary|error {
        string resourcePath = string `/v2.1/current_user/notary`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Gets notary jurisdictions for a user.
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listNotaryJournals(map<string|string[]> headers = {}, *ListNotaryJournalsQueries queries) returns NotaryJournalList|error {
        string resourcePath = string `/v2.1/current_user/notary/journals`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Returns a list of jurisdictions that the notary is registered in.
    #
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listNotaryJurisdictions(map<string|string[]> headers = {}) returns NotaryJurisdictionList|error {
        string resourcePath = string `/v2.1/current_user/notary/jurisdictions`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Creates a jurisdiction object.
    #
    # + headers - Headers to be sent with the request 
    # + payload - Notary jurisdiction used to create a jurisdiction object 
    # + return - A successful response or an error 
    remote isolated function createNotaryJurisdiction(NotaryJurisdiction payload, map<string|string[]> headers = {}) returns NotaryJurisdiction|error {
        string resourcePath = string `/v2.1/current_user/notary/jurisdictions`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->post(resourcePath, request, headers);
    }

    # Gets a jurisdiction object for the current user. The user must be a notary.
    #
    # + jurisdictionId - The ID of the jurisdiction.
    # The following jurisdictions
    # are supported:
    # -  `5 - California`
    # -  `6 - Colorado`
    # -  `9 - Florida`
    # -  `10 - Georgia`
    # -  `12 - Idaho`
    # -  `13 - Illinois`
    # -  `14 - Indiana`
    # -  `15 - Iowa`
    # -  `17 - Kentucky`
    # -  `23 - Minnesota`
    # -  `25 - Missouri`
    # -  `30 - New Jersey`
    # -  `32 - New York`
    # -  `33 - North Carolina`
    # -  `35 - Ohio`
    # -  `37 - Oregon`
    # -  `38 - Pennsylvania`
    # -  `40 - South Carolina`
    # -  `43 - Texas`
    # -  `44 - Utah`
    # -  `47 - Washington`
    # -  `48 - West Virginia`
    # -  `49 - Wisconsin`
    # -  `62 - Florida Commissioner of Deeds`
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getNotaryJurisdiction(string jurisdictionId, map<string|string[]> headers = {}) returns NotaryJurisdiction|error {
        string resourcePath = string `/v2.1/current_user/notary/jurisdictions/${getEncodedUri(jurisdictionId)}`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Updates the jurisdiction information about a notary.
    #
    # + jurisdictionId - The ID of the jurisdiction.
    # The following jurisdictions
    # are supported:
    # -  `5 - California`
    # -  `6 - Colorado`
    # -  `9 - Florida`
    # -  `10 - Georgia`
    # -  `12 - Idaho`
    # -  `13 - Illinois`
    # -  `14 - Indiana`
    # -  `15 - Iowa`
    # -  `17 - Kentucky`
    # -  `23 - Minnesota`
    # -  `25 - Missouri`
    # -  `30 - New Jersey`
    # -  `32 - New York`
    # -  `33 - North Carolina`
    # -  `35 - Ohio`
    # -  `37 - Oregon`
    # -  `38 - Pennsylvania`
    # -  `40 - South Carolina`
    # -  `43 - Texas`
    # -  `44 - Utah`
    # -  `47 - Washington`
    # -  `48 - West Virginia`
    # -  `49 - Wisconsin`
    # -  `62 - Florida Commissioner of Deeds`
    # + headers - Headers to be sent with the request 
    # + payload - Notary jurisdiction used to update the jurisdiction information about a notary 
    # + return - A successful response or an error 
    remote isolated function updateNotaryJurisdiction(string jurisdictionId, NotaryJurisdiction payload, map<string|string[]> headers = {}) returns NotaryJurisdiction|error {
        string resourcePath = string `/v2.1/current_user/notary/jurisdictions/${getEncodedUri(jurisdictionId)}`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }

    # Deletes the specified jurisdiction.
    #
    # + jurisdictionId - The ID of the jurisdiction.
    # The following jurisdictions
    # are supported:
    # -  `5 - California`
    # -  `6 - Colorado`
    # -  `9 - Florida`
    # -  `10 - Georgia`
    # -  `12 - Idaho`
    # -  `13 - Illinois`
    # -  `14 - Indiana`
    # -  `15 - Iowa`
    # -  `17 - Kentucky`
    # -  `23 - Minnesota`
    # -  `25 - Missouri`
    # -  `30 - New Jersey`
    # -  `32 - New York`
    # -  `33 - North Carolina`
    # -  `35 - Ohio`
    # -  `37 - Oregon`
    # -  `38 - Pennsylvania`
    # -  `40 - South Carolina`
    # -  `43 - Texas`
    # -  `44 - Utah`
    # -  `47 - Washington`
    # -  `48 - West Virginia`
    # -  `49 - Wisconsin`
    # -  `62 - Florida Commissioner of Deeds`
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteNotaryJurisdiction(string jurisdictionId, map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/current_user/notary/jurisdictions/${getEncodedUri(jurisdictionId)}`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Gets membership account password rules.
    #
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getCurrentUserPasswordRules(map<string|string[]> headers = {}) returns UserPasswordRules|error {
        string resourcePath = string `/v2.1/current_user/password_rules`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Gets the API request logging log files.
    #
    # + headers - Headers to be sent with the request 
    # + queries - Queries to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function listRequestLogs(map<string|string[]> headers = {}, *ListRequestLogsQueries queries) returns ApiRequestLogsResult|error {
        string resourcePath = string `/v2.1/diagnostics/request_logs`;
        resourcePath = resourcePath + check getPathForQueryParam(queries);
        return self.clientEp->get(resourcePath, headers);
    }

    # Deletes the request log files.
    #
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function deleteRequestLogs(map<string|string[]> headers = {}) returns error? {
        string resourcePath = string `/v2.1/diagnostics/request_logs`;
        return self.clientEp->delete(resourcePath, headers = headers);
    }

    # Gets a request logging log file.
    #
    # + requestLogId - The ID of the log entry
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getRequestLog(string requestLogId, map<string|string[]> headers = {}) returns string|error {
        string resourcePath = string `/v2.1/diagnostics/request_logs/${getEncodedUri(requestLogId)}`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Gets the API request logging settings.
    #
    # + headers - Headers to be sent with the request 
    # + return - A successful response or an error 
    remote isolated function getRequestLogSettings(map<string|string[]> headers = {}) returns DiagnosticsSettingsInformation|error {
        string resourcePath = string `/v2.1/diagnostics/settings`;
        return self.clientEp->get(resourcePath, headers);
    }

    # Enables or disables API request logging for troubleshooting.
    #
    # + headers - Headers to be sent with the request 
    # + payload - Diagnostics settings information used to enable or disable API request logging for troubleshooting 
    # + return - A successful response or an error 
    remote isolated function updateRequestLogSettings(DiagnosticsSettingsInformation payload, map<string|string[]> headers = {}) returns DiagnosticsSettingsInformation|error {
        string resourcePath = string `/v2.1/diagnostics/settings`;
        http:Request request = new;
        json jsonBody = jsondata:toJson(payload);
        request.setPayload(jsonBody, "application/json");
        return self.clientEp->put(resourcePath, request, headers);
    }
}
